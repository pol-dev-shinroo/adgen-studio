# Progress Log

## 2026-07-22 — Initial scaffold (Claude Cowork)
- Converted the original `광고생성기_UI목업.html` mockup into a componentized React + Vite app.
- Structure: one Context per feature area for state, components split by screen, CSS split per screen (see CLAUDE.md for the full layout).
- Not yet installed/run — the Cowork sandbox has no network access to the npm registry, so `npm install` couldn't be verified there. Manually checked all imports/exports resolve and braces/parens balance across all 41 source files.
- Two intentional deviations from the mockup, documented in README.md: gallery auto-populates a new "생성중" card when you click "생성 시작" (the original mockup left the gallery static); gallery filter chips are wired up to actually filter (were dead in the original markup).
- Open items handed to Claude Code: first real `npm install && npm run dev` verification, initialize git, create a public GitHub repo and push, then connect the repo to Vercel for auto-deploy on push.

## 2026-07-22 — First-run verification + GitHub push blocked (Claude Code)
- `npm install` + `npm run dev` confirmed clean: loads at http://localhost:3000 with zero console errors (checked all 4 screens — feed, studio wizard, gallery, settings). No build/runtime fixes were needed; the scaffold from the prior session was already correct.
- GitHub push blocked: `gh` (GitHub CLI) is not installed on this machine, so `gh auth status` fails before auth can even be checked. Did not run `git init`/commit yet, per the plan's "stop and tell me" branch.
- Open item: user needs to either install + `gh auth login` the GitHub CLI, or create the `adgen-studio` repo manually (public) at github.com/new — then Claude Code can do `git init`, first commit, `git remote add origin <url>`, `git push -u origin main`.
- Vercel connection still untouched, per instructions — user will do that manually via the dashboard once the repo exists.

## 2026-07-22 — GitHub repo created and pushed (Claude Code)
- GitHub CLI was installed since the last session (needed the full path `C:\Program Files\GitHub CLI\gh.exe` — not yet on this shell's PATH); authenticated as `pol-dev-shinroo`.
- Caught and fixed a near-miss: first `git init` accidentally ran at the parent `image-automation` folder (would have pushed local Claude settings + the original mockup HTML to a public repo). Deleted that `.git` and re-initialized inside `adgen-studio/` only.
- Initial commit (54 files) pushed to **https://github.com/pol-dev-shinroo/adgen-studio** (public, branch `main`).
- Remaining: user connects the repo to Vercel via dashboard (Import Git Repository) for auto-deploy on push.

## 2026-07-22 — Vercel connected, deployment confirmed live (Claude Cowork)
- User imported `pol-dev-shinroo/adgen-studio` into Vercel via the dashboard.
- Verified via the Vercel MCP connector (`get_project`): framework auto-detected as `vite`, latest deployment `readyState: READY`, `target: production`.
- Live URL: https://adgen-studio-red.vercel.app
- Pipeline is now fully wired: push to `main` → Vercel auto-builds → auto-deploys. No further manual deploy steps needed.
- Full loop confirmed working end-to-end: Cowork plans/scaffolds → Claude Code implements/verifies/pushes → Vercel auto-deploys → Cowork can check status via the connector without hopping tools.

## 2026-07-22 — Native Meta Ads collection backend, n8n dropped (Claude Code)
- **Decision: n8n is dropped entirely.** The Meta Ads collection pipeline is now a native Express backend under `backend/`, replicating the logic of the old n8n workflow (`(벡엔드) 1. Meta Ads v2.json`, kept outside the repo as reference).
- Flow: `POST /api/collect {keywords}` → 202 + jobId (job runs in background, in-memory job store) → Apify `facebook-ads-scraper` sync actor per keyword → pure mapper to the 20-column layout → Google Sheets upsert matched on Ad Archive ID. `GET /api/collect/:jobId` for status, `GET /api/health`.
- **Sheets auth pivoted mid-session from service-account key to OAuth2 + refresh token** — org policy `iam.disableServiceAccountKeyCreation` blocks key creation. One-time `scripts/google-auth.js` helper obtains the refresh token (localhost:3001 callback). Note: consent screen is in Testing mode → Google expires refresh tokens after ~7 days; publish to Production or re-run the script on `invalid_grant`.
- Mapper unit tests (node built-in runner): 4/4 pass — camelCase fixture, snake_case variants, near-empty item, column order.
- Real end-to-end run verified with keyword `뉴트리원`: 77 ads fetched, 77 appended / 0 updated (first run), sheet confirmed at 78 rows incl. header; existing sheet header matched our 20 columns exactly. Sample rows spot-checked — field alignment correct (note: keyword search also pulls third-party ads mentioning the keyword, e.g. Naver smartstore DPA ads).
- Secrets: `backend/.env` gitignored (verified with `git check-ignore` before commit); no tokens in code or logs.
- Frontend still calls nothing — wiring the feed screen's "실시간 수집" to `POST /api/collect` is the natural next step.

## 2026-07-23 — Drive media archiving (fbcdn URLs expire) (Claude Code)
- Problem solved: fbcdn URLs in the sheet are signed/short-lived. Media is now downloaded at collection time (signatures still fresh) and archived permanently to Google Drive: `AdGen Media Archive/<brand>/<adArchiveId>_<index>.<ext>`, "anyone with link" viewer permission.
- Sheet grew to 22 columns (A..V): `Archived Image Links` / `Archived Thumbnail` inserted at O/P after `Image Links`; original fbcdn columns kept for reference. Existing sheet migrated with `scripts/migrate-sheet-22cols.js` (inserts columns so old rows stay aligned; idempotent).
- OAuth re-scoped: refresh token now carries `spreadsheets` + `drive.file` (user re-ran `scripts/google-auth.js`); **Drive API had to be enabled in the Cloud project** — the token had the scope but the API was off, failing with "has not been used in project... or it is disabled".
- New `drive.service.js` (folder ensure/cache, per-brand subfolders, streaming upload, re-scrape dedupe via cached folder listing), `utils/pool.js` (3 parallel downloads per ad, no new deps), shared `google.client.js` for Sheets+Drive auth. Download failures log + leave cell empty, never fail the job.
- Verified with a real `뉴트리원` run: 77 ads → 0 appended / **77 updated** (upsert matching proven on re-scrape), **161 files uploaded, 0 failed**, Drive links confirmed in sheet cells O/P and click-verified.
- Video files still not archived (thumbnails only) — revisit if needed; consider Drive storage quota if many brands are collected.

## 2026-07-23 — Column reorder + destructive full reset (Claude Code)
- Reordered `AD_COLUMNS` so archived (permanent Drive) links are primary and raw fbcdn links are reference-only: `Archived Image Links` now sits before `Image Links`, `Archived Thumbnail` before `Video Thumbnail`. Still 22 columns A..V; `sheets.service.js` `LAST_COLUMN` untouched (already `'V'`). Mapper tests updated for the new indices, still 4/4 passing.
- **Full reset, client-approved**: new `scripts/reset-archive.js` clears the whole sheet tab (A:V, header included) and trashes (not permanently deletes — 30-day Drive trash recovery) the old "AdGen Media Archive" folder tree. Ran it: sheet cleared, old root folder (id `1H_dMcQNQN3xe5Wky4Sk5etktDr3pNtla`) trashed.
- Restarted the backend process afterward — `drive.service.js` caches folder IDs in memory for process lifetime, so a stale process would've kept uploading into the trashed folder. Confirmed a fresh process recreates "AdGen Media Archive" cleanly (new id `1CzOIlVVR_oNpWaoLf9YeHTkBi1a4a8Hj`) with no manual folder cleanup needed.
- Fresh end-to-end verification with keyword `안티칼` on the reset archive: 15 ads → 15 appended / 0 updated, 24 files uploaded / 0 failed. Header row confirmed in the new column order; sample Drive links (brand `antical.1`) click-verified.
- `reset-archive.js` kept in the repo as a reusable admin script (no secrets — reads env like everything else).

## 2026-07-23 — Keyword-named Drive folders, frontend wired to real backend (Claude Code)
- **Drive folders now grouped by search keyword, not Page name** — a real Facebook Page literally named "antical.1" made it obvious that grouping by the ad's raw `Brand` was the wrong axis. Renamed `getBrandFolder`→`getKeywordFolder` (drive.service.js) and the `brand`→`keyword` param throughout collect.service.js/drive.service.js. **Accepted tradeoff, client's explicit choice**: ads from different real advertisers that both match the same search term now land in the same folder — noted inline in code and here, not something to "fix" later.
- Ran the full reset again (sheet cleared, old folder trashed) and a fresh `안티칼` collection to verify: 15 ads, 24 files, 0 failures, Drive folder now literally named `안티칼` with images directly inside (previously `antical.1`).
- **Frontend now talks to the real backend for the Feed screen** (경쟁사 광고 피드 only — Studio/Gallery/Settings intentionally untouched, their real data sources don't exist yet):
  - Backend: added `cors` (env-configurable `CORS_ORIGIN`, defaults to the Vite dev server origin) and `GET /api/ads` (reads the sheet, returns rows as objects keyed by the 22-column layout) — reused `sheets.service.js`, no new auth path.
  - Frontend: new `src/api/backendClient.js` (getAds/startCollect/getJobStatus) and `src/api/adaptAd.js`. **Design choice**: adapted the real sheet-row shape into the mock UI shape (id/brand/title/gradient/media/desc/live/image) rather than reshaping AdCard/AdGrid/CollectedResults/BrandFilterBar — one adapter file is far less invasive than touching four components. `initialAds.js` kept as a fallback if the backend is unreachable on mount (not as the default state anymore).
  - `AdsContext.collect()` now does the real thing: POST to start a job, poll every 1.5s, re-fetch `/api/ads` on completion, and diff by Ad Archive ID against what the UI already had to drive the fresh/dup styling — same UX, real data.
  - **Bug caught before shipping**: the sheet stores Drive's `webViewLink` (an HTML viewer page, `.../file/d/<id>/view`), which does **not** render as `<img src>` — confirmed via `naturalWidth: 0`-style failure before the fix. `adaptAd.js` now converts it to Drive's public thumbnail endpoint (`drive.google.com/thumbnail?id=<id>&sz=w1000`) for the `<img>` tag; the sheet itself still stores the original webViewLink (the human-clickable link), unchanged.
  - `Thumb.jsx` renders a real `<img object-fit:cover>` when an ad has an archived image, with a dark gradient overlay (new CSS in `feed.css`, scoped to `.ad .thumb.has-image`) so the title/badges stay legible over a photo; falls back to the original gradient box when there's no image. Change kept localized — gallery/studio's separate raw `.thumb` divs are untouched.
  - Verified live in-browser with both dev servers running: feed loads 15 real `antical.1`/`안티칼` ads with real photos (network-confirmed `naturalWidth: 1000`), zero console errors, and a real "실시간 수집" run for `안티칼` correctly reported 15/15 as duplicates (already archived) with the right badges. Side effect (not a bug): Studio's step-1 reference-brand list now shows real brands too, since it reads the same shared `AdsContext` — Studio's own code was not modified.
  - One test-only false alarm worth recording: the browser automation's "type" action fired a trailing Enter that double-submitted the search once, racing two collection jobs — not a code bug, resolved by using a direct value-set instead. Left a `console.error` in `collect()`'s catch block (previously silent) since it was genuinely useful for debugging and has no downside.

## 2026-07-24 — Feed card redesign: compact + detail modal, persisted rename (Claude Code)
- **Problem**: real sheet data has 22 fields; the feed card was designed for 6 mock fields and had gotten overloaded. Fix: compact browsing card, click-through modal for the full record.
- `adaptAd.js`: now attaches `raw: ad` (the original sheet row) to every adapted ad, plus `images` (full archived-image array, not just the first) and `pageName` (the ad's actual Facebook Page name, kept separate). **Brand label switched from the raw Page name to Search Keyword** (falls back to Brand, then a placeholder) — matches how Drive folders are already grouped, and stops a page literally named "antical.1" from being the primary label. `toEmbeddableImageUrl` (webViewLink → Drive thumbnail URL) is now exported for reuse by the detail modal's gallery.
- **Rename now persists** instead of being local-only: new `sheets.service.js` `updateAdField(adArchiveId, columnName, value)` (finds the row the same way `upsertAdRows` does, computes the column letter from `AD_COLUMNS.indexOf`, single-cell `values.update`) behind `PATCH /api/ads/:adArchiveId`, allowlisted to `field: 'Search Keyword'` only — everything else in the sheet stays scraper-owned, rejected with 400. `AdsContext.renameBrand` is now async: optimistic update, rollback + error toast on failure. Verified: renamed via the pencil icon, confirmed the change **survives a full page reload** (round-tripped through the real sheet), then renamed back.
- New reusable `components/common/Modal.jsx` (overlay + dialog, closes on backdrop click or Escape) with shared styles in `global.css` since it's generic UI, not feed-specific.
- `AdCard.jsx` simplified: dropped the `MultiLineText` title overlay from the photo (badges only now); body is brand+pencil, a muted Page-name subline (only when it differs from the keyword-brand), one ellipsis-truncated copy line, the existing meta line, then the CTA. Whole card is clickable (`onOpenDetail(ad)`); pencil and CTA call `e.stopPropagation()` so they don't also pop the modal — verified via dispatched click events (backdrop-click and Escape close correctly, pencil/CTA clicks correctly do *not* open the modal).
- New `AdDetailModal.jsx` renders `ad.raw` in full: media gallery + video link, header (brand/pageName/status/format/platforms/variant count), copy (title/post/bottom/CTA/landing URL), dates (start/end/scraped/keyword), footer (Ad Library URL, Ad Archive ID, the same "이 광고로 생성하기" action). Wired through plain prop drilling (`FeedScreen` owns `selectedAd` state, passes `onOpenDetail` down through `AdGrid`/`CollectedResults` to `AdCard`) — no new context, per the plan.
- Verified live with both dev servers: cards show `안티칼` as the primary label (not `antical.1`), modal opens with correct data on card click, closes on Escape/backdrop, pencil/CTA don't trigger it, and a rename round-tripped through a real page reload. Browser-automation flakiness this session (stale element refs, a scroll gesture that spuriously closed the modal) was tooling noise, not app bugs — confirmed by re-testing via dispatched DOM events instead of coordinate-based clicks.

## 2026-07-24 — Real re-scrape diffing, wired filters, image lightbox (Claude Code)
- **`upsertAdRows` now genuinely diffs re-scrapes** instead of always reporting "updated": fetches the full existing range once (not just column A), compares each matched row against what was already there, and classifies new/updated/unchanged. Returns `{ appended, updated, unchanged, statuses: [{adArchiveId, status, changedFields}] }`. `collect.service.js` aggregates `statuses`/`unchanged` into `job.summary` across all keywords in a job; `GET /api/collect/:jobId` already returned `summary` wholesale so no controller change was needed.
- **Deviated from the literal spec on purpose, backed by empirical evidence**: was told to exclude only `Date Scraped` from the diff. Tested that literally first — re-collecting `안티칼` twice showed **100% "updated"**, because the raw `Image Links`/`Video Link`/`Video Thumbnail` columns hold Facebook's signed CDN URLs, which get a fresh signature on every single scrape regardless of whether the ad changed at all. That directly contradicted the task's own verification goal ("confirm the second run reports mostly unchanged"), so those three volatile raw-CDN columns are now excluded from the diff alongside Date Scraped — the stable Drive-hosted `Archived ...` counterparts still count. Re-verified after the fix: two consecutive `안티칼` runs both landed at **14 unchanged / 1 updated** (that one ad's `End Date` genuinely differs, a legitimate diff) — consistent and correct.
- **Frontend trusts the backend's classification** instead of re-deriving it: `AdsContext.collect()` builds a `Map` from `job.summary.statuses` and reads `status`/`changedFields` off it per ad, replacing the old local `isDup` guess (which only ever compared against whatever the UI happened to have cached). `collected` ads now carry `status: 'new'|'updated'|'unchanged'`. `CollectedResults.jsx` renders three dupnote variants (new — unchanged style; updated — amber "♻ 정보 업데이트됨" with changed fields translated to friendly Korean labels where one's defined, e.g. "종료일"; unchanged — reworded "🗂 이미 아카이브된 광고 — 변경 사항 없음"). Toast and summary line both report three counts now. Verified live: toast read "신규 0건 · 업데이트 1건 · 변경없음 14건", matching the API-level result exactly.
- **Feed filter chips wired up** (archive tab only, per instructions): `AdsContext` gained `mediaFilter` ('all'/'image'/'video', toggle-to-reset-on-repeat-click like other chips in the app) and `recentOnly` (last 7 days by `raw['Date Scraped']`). `AdGrid.jsx` applies brand + media + recency together. Verified each chip live via dispatched click events: image/video filters toggle and reset correctly, recency filter applies and resets.
- **Full-size image lightbox**: new `components/common/ImageLightbox.jsx`, reusing `Modal.jsx` (which gained optional `overlayClassName`/`dialogClassName` props, backward compatible). Takes the *original* Drive webViewLinks (already sitting in `ad.images` — nothing new needed threading through `adaptAd.js`, just reused as-is) and builds both the larger preview (`toEmbeddableImageUrl(link, 'w1600')` — that helper gained an optional `size` param, default `w1000` preserves every other call site) and the "원본 파일 열기" link from the same array. Supports left/right arrow-key navigation and Escape.
  - **Stacked-modal Escape conflict, solved without touching Modal's internals**: two `Modal` instances both register their own `document` keydown listener, so a naive stack would close both the lightbox and the detail view on one Escape press (the outer one, mounted first, fires first in listener-registration order). Fixed by wrapping `AdDetailModal`'s own `onClose` in a guard that checks local `lightboxIndex` state first — if the lightbox is open, the first Escape (or backdrop click, or the ✕ button) just closes the lightbox; a second closes the detail view. Verified via dispatched keydown events: confirmed the exact two-step sequence, plus arrow-key navigation, boundary button disabling, and a real `naturalWidth: 1600` image load.

## 2026-07-25 — Live collection progress, size slider, rate-limit retries, image retry (Claude Code)
- **Granular job progress**: `collect.service.js`'s job object gained a `progress` field (`phase: 'idle'|'scraping'|'archiving'|'saving'|'done'`, `currentKeyword`, `totalAdsFound`, `adsProcessed`, `recentItems[]` capped at 20, newest first). Each `recentItems` entry starts `status: 'processing'` the moment an ad finishes archiving, then gets retroactively patched to its real `'new'|'updated'|'unchanged'` once `upsertAdRows` classifies it — so the live list shows a believable in-progress state without lying about the final verdict. `GET /api/collect/:jobId` now returns `progress` (and `errorCode`) alongside `summary`.
- **Frontend polls live instead of only reading the final result**: `AdsContext.collect()` now sets a placeholder `activeJob` *synchronously*, before even awaiting `startCollect`'s network response — this is what makes the tab switch to "실시간 수집 결과" instant rather than waiting on the first poll. Every poll tick (now 900ms, down from 1.5s) calls `setActiveJob(job)` with the real server state; `FeedScreen` renders the new `CollectionProgress.jsx` (indeterminate bar while scraping, real percentage bar while archiving, live newest-first card list with thumbnail/brand/snippet/status chip) in place of the idle text or final grid for as long as `activeJob` is non-null. Verified live: the progress UI was visibly present in the DOM in the same tick as the click (confirmed via a JS check run immediately after dispatching the click), and a real run showed the percentage bar and list advancing through several intermediate states (e.g. 3/20 → 19/20) before handing off.
- **"모두 최신 상태입니다" friendly line**: when a completed run has zero new and zero updated ads, `CollectedResults.jsx` now leads with that plain-language sentence above the bare counts. Verified by re-collecting an already-fully-archived keyword three times in a row until it landed at 0 new / 0 updated — the line appeared exactly then, and not on the two earlier runs that still had new/changed ads.
- **User-controlled result size**: a 10–200 (step 10, default 50) slider next to the search bar, kept as local state in `SearchBar.jsx` (judgment call over `AdsContext` — nothing else needs the value, it's only read at the moment `collect()` fires) threaded through `collect(query, resultsLimit)` → `POST /api/collect` → `collect.controller.js` (validates integer 10–200, rejects otherwise) → `startCollection`/`runJob` → `runFacebookAdsScraper`. Verified two ways: the 200-cap rejection fires correctly for out-of-range values, and — more importantly — collecting a keyword known to have 77 archived ads with `resultsLimit: 20` genuinely came back with exactly 20, not the full set.
- **Rate-limit resilience**: new `backend/src/utils/retry.js` (`withRetry` — exponential backoff, 3 retries default; `googleIsRetryable` — classifies googleapis errors by HTTP 429 or a 403 with a `rateLimitExceeded`/`userRateLimitExceeded` reason, tagging them `.retryable`/`.code = 'RATE_LIMITED'`). Wired into every Apify/Sheets/Drive API call (`apify.service.js` marks 429→`RATE_LIMITED`/5xx→`UPSTREAM_ERROR` before rethrowing; `sheets.service.js`/`drive.service.js` wrap each call through a local `callSheets`/`callDrive` helper). Job-level failure now sets a clearer `job.error` + `job.errorCode` for `RATE_LIMITED`, and the frontend shows a distinct toast for it.
  - **One deviation from the literal instruction, for correctness**: `drive.service.js` was asked to wrap `files.create` directly, but that calls streams the downloaded response body straight into the upload — a stream can only be read once, so retrying `files.create` alone with an already-consumed (or partially-consumed) stream would silently corrupt the retried upload. Restructured so the *download + files.create* retry together, re-fetching the source URL fresh on every attempt; `permissions.create` (no body, always safe) is retried on its own as asked. Also: the task named `files.update` as one of the calls to wrap, but no such call exists anywhere in `drive.service.js` — skipped, nothing to wrap.
  - Verified the happy path still works end-to-end (every collection run in this session's testing succeeded normally through the wrapped calls) and that the code loads/runs cleanly; did not attempt to manufacture a real 429 to prove the retry branch itself, per the task's own instruction that this wasn't necessary.
- **Thumbnail images retry before falling back to the gradient**: new `components/common/RetryImage.jsx` (up to 2 retries, 600ms/1800ms backoff, cache-busting `&retry=N` query param, `loading="lazy"`). Swapped into `Thumb.jsx` (card thumbnails — `onFinalError` flips a local `imageFailed` flag so the wrapper reverts to its existing gradient-class fallback, with a `useEffect` reset so a *different* image on the same card instance isn't permanently stuck showing the gradient from a previous failure), `AdDetailModal.jsx`'s gallery, and `ImageLightbox.jsx`'s large image (judgment call — small change, same throttling risk applies to full-size loads). Stress-tested against a 147-card grid built up over this session's test collections: all currently-visible (non-lazy-deferred) thumbnails loaded successfully across three separate reloads, zero gradient fallbacks, zero broken images.
