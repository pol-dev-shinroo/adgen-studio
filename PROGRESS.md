# Progress Log

## 2026-07-22 — Initial scaffold (Claude Cowork)
- Converted the original `광고생성기_UI목업.html` mockup into a componentized React + Vite app.
- Structure: one Context per feature area for state, components split by screen, CSS split per screen (see CLAUDE.md for the full layout).
- Not yet installed/run — the Cowork sandbox has no network access to the npm registry, so `npm install` couldn't be verified there. Manually checked all imports/exports resolve and braces/parens balance across all 41 source files.
- Two intentional deviations from the mockup, documented in README.md: gallery auto-populates a new "생성중" card when you click "생성 시작" (the original mockup left the gallery static); gallery filter chips are wired up to actually filter (were dead in the original markup).
- Open items handed to Claude Code: first real `npm install && npm run dev` verification, initialize git, create a public GitHub repo and push, then connect the repo to Vercel for auto-deploy on push.

## 2026-07-22 — First-run verification + GitHub push blocked (Claude Code)
- `npm install` + `npm run dev` confirmed clean: loads at http://localhost:3000 with zero console errors (checked all 4 screens — feed, studio wizard, gallery, settings). No build/runtime fixes were needed; the scaffold from the prior session was already correct.
- GitHub push blocked: `gh` (GitHub CLI) is not installed on this machine, so `gh auth status` fails before auth can even be checked. Did not run `git init`/commit yet, per the plan's "stop and tell me" branch.
- Open item: user needs to either install + `gh auth login` the GitHub CLI, or create the `adgen-studio` repo manually (public) at github.com/new — then Claude Code can do `git init`, first commit, `git remote add origin <url>`, `git push -u origin main`.
- Vercel connection still untouched, per instructions — user will do that manually via the dashboard once the repo exists.

## 2026-07-22 — GitHub repo created and pushed (Claude Code)
- GitHub CLI was installed since the last session (needed the full path `C:\Program Files\GitHub CLI\gh.exe` — not yet on this shell's PATH); authenticated as `pol-dev-shinroo`.
- Caught and fixed a near-miss: first `git init` accidentally ran at the parent `image-automation` folder (would have pushed local Claude settings + the original mockup HTML to a public repo). Deleted that `.git` and re-initialized inside `adgen-studio/` only.
- Initial commit (54 files) pushed to **https://github.com/pol-dev-shinroo/adgen-studio** (public, branch `main`).
- Remaining: user connects the repo to Vercel via dashboard (Import Git Repository) for auto-deploy on push.

## 2026-07-22 — Vercel connected, deployment confirmed live (Claude Cowork)
- User imported `pol-dev-shinroo/adgen-studio` into Vercel via the dashboard.
- Verified via the Vercel MCP connector (`get_project`): framework auto-detected as `vite`, latest deployment `readyState: READY`, `target: production`.
- Live URL: https://adgen-studio-red.vercel.app
- Pipeline is now fully wired: push to `main` → Vercel auto-builds → auto-deploys. No further manual deploy steps needed.
- Full loop confirmed working end-to-end: Cowork plans/scaffolds → Claude Code implements/verifies/pushes → Vercel auto-deploys → Cowork can check status via the connector without hopping tools.

## 2026-07-22 — Native Meta Ads collection backend, n8n dropped (Claude Code)
- **Decision: n8n is dropped entirely.** The Meta Ads collection pipeline is now a native Express backend under `backend/`, replicating the logic of the old n8n workflow (`(벡엔드) 1. Meta Ads v2.json`, kept outside the repo as reference).
- Flow: `POST /api/collect {keywords}` → 202 + jobId (job runs in background, in-memory job store) → Apify `facebook-ads-scraper` sync actor per keyword → pure mapper to the 20-column layout → Google Sheets upsert matched on Ad Archive ID. `GET /api/collect/:jobId` for status, `GET /api/health`.
- **Sheets auth pivoted mid-session from service-account key to OAuth2 + refresh token** — org policy `iam.disableServiceAccountKeyCreation` blocks key creation. One-time `scripts/google-auth.js` helper obtains the refresh token (localhost:3001 callback). Note: consent screen is in Testing mode → Google expires refresh tokens after ~7 days; publish to Production or re-run the script on `invalid_grant`.
- Mapper unit tests (node built-in runner): 4/4 pass — camelCase fixture, snake_case variants, near-empty item, column order.
- Real end-to-end run verified with keyword `뉴트리원`: 77 ads fetched, 77 appended / 0 updated (first run), sheet confirmed at 78 rows incl. header; existing sheet header matched our 20 columns exactly. Sample rows spot-checked — field alignment correct (note: keyword search also pulls third-party ads mentioning the keyword, e.g. Naver smartstore DPA ads).
- Secrets: `backend/.env` gitignored (verified with `git check-ignore` before commit); no tokens in code or logs.
- Frontend still calls nothing — wiring the feed screen's "실시간 수집" to `POST /api/collect` is the natural next step.

## 2026-07-23 — Drive media archiving (fbcdn URLs expire) (Claude Code)
- Problem solved: fbcdn URLs in the sheet are signed/short-lived. Media is now downloaded at collection time (signatures still fresh) and archived permanently to Google Drive: `AdGen Media Archive/<brand>/<adArchiveId>_<index>.<ext>`, "anyone with link" viewer permission.
- Sheet grew to 22 columns (A..V): `Archived Image Links` / `Archived Thumbnail` inserted at O/P after `Image Links`; original fbcdn columns kept for reference. Existing sheet migrated with `scripts/migrate-sheet-22cols.js` (inserts columns so old rows stay aligned; idempotent).
- OAuth re-scoped: refresh token now carries `spreadsheets` + `drive.file` (user re-ran `scripts/google-auth.js`); **Drive API had to be enabled in the Cloud project** — the token had the scope but the API was off, failing with "has not been used in project... or it is disabled".
- New `drive.service.js` (folder ensure/cache, per-brand subfolders, streaming upload, re-scrape dedupe via cached folder listing), `utils/pool.js` (3 parallel downloads per ad, no new deps), shared `google.client.js` for Sheets+Drive auth. Download failures log + leave cell empty, never fail the job.
- Verified with a real `뉴트리원` run: 77 ads → 0 appended / **77 updated** (upsert matching proven on re-scrape), **161 files uploaded, 0 failed**, Drive links confirmed in sheet cells O/P and click-verified.
- Video files still not archived (thumbnails only) — revisit if needed; consider Drive storage quota if many brands are collected.

## 2026-07-23 — Column reorder + destructive full reset (Claude Code)
- Reordered `AD_COLUMNS` so archived (permanent Drive) links are primary and raw fbcdn links are reference-only: `Archived Image Links` now sits before `Image Links`, `Archived Thumbnail` before `Video Thumbnail`. Still 22 columns A..V; `sheets.service.js` `LAST_COLUMN` untouched (already `'V'`). Mapper tests updated for the new indices, still 4/4 passing.
- **Full reset, client-approved**: new `scripts/reset-archive.js` clears the whole sheet tab (A:V, header included) and trashes (not permanently deletes — 30-day Drive trash recovery) the old "AdGen Media Archive" folder tree. Ran it: sheet cleared, old root folder (id `1H_dMcQNQN3xe5Wky4Sk5etktDr3pNtla`) trashed.
- Restarted the backend process afterward — `drive.service.js` caches folder IDs in memory for process lifetime, so a stale process would've kept uploading into the trashed folder. Confirmed a fresh process recreates "AdGen Media Archive" cleanly (new id `1CzOIlVVR_oNpWaoLf9YeHTkBi1a4a8Hj`) with no manual folder cleanup needed.
- Fresh end-to-end verification with keyword `안티칼` on the reset archive: 15 ads → 15 appended / 0 updated, 24 files uploaded / 0 failed. Header row confirmed in the new column order; sample Drive links (brand `antical.1`) click-verified.
- `reset-archive.js` kept in the repo as a reusable admin script (no secrets — reads env like everything else).

## 2026-07-23 — Keyword-named Drive folders, frontend wired to real backend (Claude Code)
- **Drive folders now grouped by search keyword, not Page name** — a real Facebook Page literally named "antical.1" made it obvious that grouping by the ad's raw `Brand` was the wrong axis. Renamed `getBrandFolder`→`getKeywordFolder` (drive.service.js) and the `brand`→`keyword` param throughout collect.service.js/drive.service.js. **Accepted tradeoff, client's explicit choice**: ads from different real advertisers that both match the same search term now land in the same folder — noted inline in code and here, not something to "fix" later.
- Ran the full reset again (sheet cleared, old folder trashed) and a fresh `안티칼` collection to verify: 15 ads, 24 files, 0 failures, Drive folder now literally named `안티칼` with images directly inside (previously `antical.1`).
- **Frontend now talks to the real backend for the Feed screen** (경쟁사 광고 피드 only — Studio/Gallery/Settings intentionally untouched, their real data sources don't exist yet):
  - Backend: added `cors` (env-configurable `CORS_ORIGIN`, defaults to the Vite dev server origin) and `GET /api/ads` (reads the sheet, returns rows as objects keyed by the 22-column layout) — reused `sheets.service.js`, no new auth path.
  - Frontend: new `src/api/backendClient.js` (getAds/startCollect/getJobStatus) and `src/api/adaptAd.js`. **Design choice**: adapted the real sheet-row shape into the mock UI shape (id/brand/title/gradient/media/desc/live/image) rather than reshaping AdCard/AdGrid/CollectedResults/BrandFilterBar — one adapter file is far less invasive than touching four components. `initialAds.js` kept as a fallback if the backend is unreachable on mount (not as the default state anymore).
  - `AdsContext.collect()` now does the real thing: POST to start a job, poll every 1.5s, re-fetch `/api/ads` on completion, and diff by Ad Archive ID against what the UI already had to drive the fresh/dup styling — same UX, real data.
  - **Bug caught before shipping**: the sheet stores Drive's `webViewLink` (an HTML viewer page, `.../file/d/<id>/view`), which does **not** render as `<img src>` — confirmed via `naturalWidth: 0`-style failure before the fix. `adaptAd.js` now converts it to Drive's public thumbnail endpoint (`drive.google.com/thumbnail?id=<id>&sz=w1000`) for the `<img>` tag; the sheet itself still stores the original webViewLink (the human-clickable link), unchanged.
  - `Thumb.jsx` renders a real `<img object-fit:cover>` when an ad has an archived image, with a dark gradient overlay (new CSS in `feed.css`, scoped to `.ad .thumb.has-image`) so the title/badges stay legible over a photo; falls back to the original gradient box when there's no image. Change kept localized — gallery/studio's separate raw `.thumb` divs are untouched.
  - Verified live in-browser with both dev servers running: feed loads 15 real `antical.1`/`안티칼` ads with real photos (network-confirmed `naturalWidth: 1000`), zero console errors, and a real "실시간 수집" run for `안티칼` correctly reported 15/15 as duplicates (already archived) with the right badges. Side effect (not a bug): Studio's step-1 reference-brand list now shows real brands too, since it reads the same shared `AdsContext` — Studio's own code was not modified.
  - One test-only false alarm worth recording: the browser automation's "type" action fired a trailing Enter that double-submitted the search once, racing two collection jobs — not a code bug, resolved by using a direct value-set instead. Left a `console.error` in `collect()`'s catch block (previously silent) since it was genuinely useful for debugging and has no downside.

## 2026-07-24 — Feed card redesign: compact + detail modal, persisted rename (Claude Code)
- **Problem**: real sheet data has 22 fields; the feed card was designed for 6 mock fields and had gotten overloaded. Fix: compact browsing card, click-through modal for the full record.
- `adaptAd.js`: now attaches `raw: ad` (the original sheet row) to every adapted ad, plus `images` (full archived-image array, not just the first) and `pageName` (the ad's actual Facebook Page name, kept separate). **Brand label switched from the raw Page name to Search Keyword** (falls back to Brand, then a placeholder) — matches how Drive folders are already grouped, and stops a page literally named "antical.1" from being the primary label. `toEmbeddableImageUrl` (webViewLink → Drive thumbnail URL) is now exported for reuse by the detail modal's gallery.
- **Rename now persists** instead of being local-only: new `sheets.service.js` `updateAdField(adArchiveId, columnName, value)` (finds the row the same way `upsertAdRows` does, computes the column letter from `AD_COLUMNS.indexOf`, single-cell `values.update`) behind `PATCH /api/ads/:adArchiveId`, allowlisted to `field: 'Search Keyword'` only — everything else in the sheet stays scraper-owned, rejected with 400. `AdsContext.renameBrand` is now async: optimistic update, rollback + error toast on failure. Verified: renamed via the pencil icon, confirmed the change **survives a full page reload** (round-tripped through the real sheet), then renamed back.
- New reusable `components/common/Modal.jsx` (overlay + dialog, closes on backdrop click or Escape) with shared styles in `global.css` since it's generic UI, not feed-specific.
- `AdCard.jsx` simplified: dropped the `MultiLineText` title overlay from the photo (badges only now); body is brand+pencil, a muted Page-name subline (only when it differs from the keyword-brand), one ellipsis-truncated copy line, the existing meta line, then the CTA. Whole card is clickable (`onOpenDetail(ad)`); pencil and CTA call `e.stopPropagation()` so they don't also pop the modal — verified via dispatched click events (backdrop-click and Escape close correctly, pencil/CTA clicks correctly do *not* open the modal).
- New `AdDetailModal.jsx` renders `ad.raw` in full: media gallery + video link, header (brand/pageName/status/format/platforms/variant count), copy (title/post/bottom/CTA/landing URL), dates (start/end/scraped/keyword), footer (Ad Library URL, Ad Archive ID, the same "이 광고로 생성하기" action). Wired through plain prop drilling (`FeedScreen` owns `selectedAd` state, passes `onOpenDetail` down through `AdGrid`/`CollectedResults` to `AdCard`) — no new context, per the plan.
- Verified live with both dev servers: cards show `안티칼` as the primary label (not `antical.1`), modal opens with correct data on card click, closes on Escape/backdrop, pencil/CTA don't trigger it, and a rename round-tripped through a real page reload. Browser-automation flakiness this session (stale element refs, a scroll gesture that spuriously closed the modal) was tooling noise, not app bugs — confirmed by re-testing via dispatched DOM events instead of coordinate-based clicks.

## 2026-07-24 — Real re-scrape diffing, wired filters, image lightbox (Claude Code)
- **`upsertAdRows` now genuinely diffs re-scrapes** instead of always reporting "updated": fetches the full existing range once (not just column A), compares each matched row against what was already there, and classifies new/updated/unchanged. Returns `{ appended, updated, unchanged, statuses: [{adArchiveId, status, changedFields}] }`. `collect.service.js` aggregates `statuses`/`unchanged` into `job.summary` across all keywords in a job; `GET /api/collect/:jobId` already returned `summary` wholesale so no controller change was needed.
- **Deviated from the literal spec on purpose, backed by empirical evidence**: was told to exclude only `Date Scraped` from the diff. Tested that literally first — re-collecting `안티칼` twice showed **100% "updated"**, because the raw `Image Links`/`Video Link`/`Video Thumbnail` columns hold Facebook's signed CDN URLs, which get a fresh signature on every single scrape regardless of whether the ad changed at all. That directly contradicted the task's own verification goal ("confirm the second run reports mostly unchanged"), so those three volatile raw-CDN columns are now excluded from the diff alongside Date Scraped — the stable Drive-hosted `Archived ...` counterparts still count. Re-verified after the fix: two consecutive `안티칼` runs both landed at **14 unchanged / 1 updated** (that one ad's `End Date` genuinely differs, a legitimate diff) — consistent and correct.
- **Frontend trusts the backend's classification** instead of re-deriving it: `AdsContext.collect()` builds a `Map` from `job.summary.statuses` and reads `status`/`changedFields` off it per ad, replacing the old local `isDup` guess (which only ever compared against whatever the UI happened to have cached). `collected` ads now carry `status: 'new'|'updated'|'unchanged'`. `CollectedResults.jsx` renders three dupnote variants (new — unchanged style; updated — amber "♻ 정보 업데이트됨" with changed fields translated to friendly Korean labels where one's defined, e.g. "종료일"; unchanged — reworded "🗂 이미 아카이브된 광고 — 변경 사항 없음"). Toast and summary line both report three counts now. Verified live: toast read "신규 0건 · 업데이트 1건 · 변경없음 14건", matching the API-level result exactly.
- **Feed filter chips wired up** (archive tab only, per instructions): `AdsContext` gained `mediaFilter` ('all'/'image'/'video', toggle-to-reset-on-repeat-click like other chips in the app) and `recentOnly` (last 7 days by `raw['Date Scraped']`). `AdGrid.jsx` applies brand + media + recency together. Verified each chip live via dispatched click events: image/video filters toggle and reset correctly, recency filter applies and resets.
- **Full-size image lightbox**: new `components/common/ImageLightbox.jsx`, reusing `Modal.jsx` (which gained optional `overlayClassName`/`dialogClassName` props, backward compatible). Takes the *original* Drive webViewLinks (already sitting in `ad.images` — nothing new needed threading through `adaptAd.js`, just reused as-is) and builds both the larger preview (`toEmbeddableImageUrl(link, 'w1600')` — that helper gained an optional `size` param, default `w1000` preserves every other call site) and the "원본 파일 열기" link from the same array. Supports left/right arrow-key navigation and Escape.
  - **Stacked-modal Escape conflict, solved without touching Modal's internals**: two `Modal` instances both register their own `document` keydown listener, so a naive stack would close both the lightbox and the detail view on one Escape press (the outer one, mounted first, fires first in listener-registration order). Fixed by wrapping `AdDetailModal`'s own `onClose` in a guard that checks local `lightboxIndex` state first — if the lightbox is open, the first Escape (or backdrop click, or the ✕ button) just closes the lightbox; a second closes the detail view. Verified via dispatched keydown events: confirmed the exact two-step sequence, plus arrow-key navigation, boundary button disabling, and a real `naturalWidth: 1600` image load.

## 2026-07-25 — Live collection progress, size slider, rate-limit retries, image retry (Claude Code)
- **Granular job progress**: `collect.service.js`'s job object gained a `progress` field (`phase: 'idle'|'scraping'|'archiving'|'saving'|'done'`, `currentKeyword`, `totalAdsFound`, `adsProcessed`, `recentItems[]` capped at 20, newest first). Each `recentItems` entry starts `status: 'processing'` the moment an ad finishes archiving, then gets retroactively patched to its real `'new'|'updated'|'unchanged'` once `upsertAdRows` classifies it — so the live list shows a believable in-progress state without lying about the final verdict. `GET /api/collect/:jobId` now returns `progress` (and `errorCode`) alongside `summary`.
- **Frontend polls live instead of only reading the final result**: `AdsContext.collect()` now sets a placeholder `activeJob` *synchronously*, before even awaiting `startCollect`'s network response — this is what makes the tab switch to "실시간 수집 결과" instant rather than waiting on the first poll. Every poll tick (now 900ms, down from 1.5s) calls `setActiveJob(job)` with the real server state; `FeedScreen` renders the new `CollectionProgress.jsx` (indeterminate bar while scraping, real percentage bar while archiving, live newest-first card list with thumbnail/brand/snippet/status chip) in place of the idle text or final grid for as long as `activeJob` is non-null. Verified live: the progress UI was visibly present in the DOM in the same tick as the click (confirmed via a JS check run immediately after dispatching the click), and a real run showed the percentage bar and list advancing through several intermediate states (e.g. 3/20 → 19/20) before handing off.
- **"모두 최신 상태입니다" friendly line**: when a completed run has zero new and zero updated ads, `CollectedResults.jsx` now leads with that plain-language sentence above the bare counts. Verified by re-collecting an already-fully-archived keyword three times in a row until it landed at 0 new / 0 updated — the line appeared exactly then, and not on the two earlier runs that still had new/changed ads.
- **User-controlled result size**: a 10–200 (step 10, default 50) slider next to the search bar, kept as local state in `SearchBar.jsx` (judgment call over `AdsContext` — nothing else needs the value, it's only read at the moment `collect()` fires) threaded through `collect(query, resultsLimit)` → `POST /api/collect` → `collect.controller.js` (validates integer 10–200, rejects otherwise) → `startCollection`/`runJob` → `runFacebookAdsScraper`. Verified two ways: the 200-cap rejection fires correctly for out-of-range values, and — more importantly — collecting a keyword known to have 77 archived ads with `resultsLimit: 20` genuinely came back with exactly 20, not the full set.
- **Rate-limit resilience**: new `backend/src/utils/retry.js` (`withRetry` — exponential backoff, 3 retries default; `googleIsRetryable` — classifies googleapis errors by HTTP 429 or a 403 with a `rateLimitExceeded`/`userRateLimitExceeded` reason, tagging them `.retryable`/`.code = 'RATE_LIMITED'`). Wired into every Apify/Sheets/Drive API call (`apify.service.js` marks 429→`RATE_LIMITED`/5xx→`UPSTREAM_ERROR` before rethrowing; `sheets.service.js`/`drive.service.js` wrap each call through a local `callSheets`/`callDrive` helper). Job-level failure now sets a clearer `job.error` + `job.errorCode` for `RATE_LIMITED`, and the frontend shows a distinct toast for it.
  - **One deviation from the literal instruction, for correctness**: `drive.service.js` was asked to wrap `files.create` directly, but that calls streams the downloaded response body straight into the upload — a stream can only be read once, so retrying `files.create` alone with an already-consumed (or partially-consumed) stream would silently corrupt the retried upload. Restructured so the *download + files.create* retry together, re-fetching the source URL fresh on every attempt; `permissions.create` (no body, always safe) is retried on its own as asked. Also: the task named `files.update` as one of the calls to wrap, but no such call exists anywhere in `drive.service.js` — skipped, nothing to wrap.
  - Verified the happy path still works end-to-end (every collection run in this session's testing succeeded normally through the wrapped calls) and that the code loads/runs cleanly; did not attempt to manufacture a real 429 to prove the retry branch itself, per the task's own instruction that this wasn't necessary.
- **Thumbnail images retry before falling back to the gradient**: new `components/common/RetryImage.jsx` (up to 2 retries, 600ms/1800ms backoff, cache-busting `&retry=N` query param, `loading="lazy"`). Swapped into `Thumb.jsx` (card thumbnails — `onFinalError` flips a local `imageFailed` flag so the wrapper reverts to its existing gradient-class fallback, with a `useEffect` reset so a *different* image on the same card instance isn't permanently stuck showing the gradient from a previous failure), `AdDetailModal.jsx`'s gallery, and `ImageLightbox.jsx`'s large image (judgment call — small change, same throttling risk applies to full-size loads). Stress-tested against a 147-card grid built up over this session's test collections: all currently-visible (non-lazy-deferred) thumbnails loaded successfully across three separate reloads, zero gradient fallbacks, zero broken images.

## 2026-07-26 — Post-collection discard, collect-button pulse + double-submit guard (Claude Code)
- **Post-collection review: select, then discard or keep.** Still auto-save-on-scrape (no staging gate) — this adds an after-the-fact undo path instead of rearchitecting the pipeline. New `sheets.service.js` `deleteAdRows(adArchiveIds)` actually removes rows via a `deleteDimension` batch (not a values-clear like the old `reset-archive.js` sheet wipe) — needed the tab's internal numeric `sheetId` (gid) for that, fetched once via `spreadsheets.get` and cached (`getSheetGid()`, same pattern as Drive's cached root folder ID). Row numbers are sorted **descending** before building the batch so a later deletion never shifts the index of an earlier one still queued in the same request. New `drive.service.js` `deleteAdMedia(keyword, adArchiveId)` finds and trashes every file for that ad in its keyword folder (same recoverable-trash convention as `reset-archive.js`) and removes them from the folder's cached `fileNames` set so a future re-scrape doesn't think a trashed file still exists. New `POST /api/ads/discard` calls both per ID, with Drive cleanup and sheet deletion in separate try/catches so a failure in one never blocks the other — everything attempted, every failure collected into the response.
- **Verified end-to-end, not just at the API level**: ran a real collection (15 `안티칼` ads), entered select mode (confirmed all 15 pre-checked, pencil/CTA hidden), unchecked 2, clicked 보관하기 — frontend correctly showed the other 13 with the right toast ("보관 13건 · 폐기 2건"). Then checked the *actual sources of truth* directly: `GET /api/ads` confirmed the sheet went from 147→145 rows with the two IDs genuinely absent (not blanked) and the remaining rows' fields still correctly aligned (proving the descending-delete-order logic works, not just that *a* row got removed); a direct Drive API check confirmed both discarded ads' files were trashed (0 active, 1 trashed each) while a kept ad's file was untouched (1 active, still shareable).
- **Frontend**: `CollectedResults.jsx` gained select mode — "선택하기" pre-checks everything (uncheck the few you don't want, rather than checking the ones you do); a checkbox replaces the status badge on each card while active; "보관하기" computes the unchecked set and calls the new `AdsContext.discardAds()`, which removes those IDs from both `collected` and `ads` on success and reports kept-vs-discarded in a toast; nothing-unchecked short-circuits to a "모든 항목이 보관되었습니다" toast without hitting the backend at all. `AdCard.jsx` gained `selectable`/`selected`/`onToggleSelect` props, all optional/inactive by default so `AdGrid`'s existing archive-tab usage is untouched — clicking a selectable card toggles selection instead of opening the detail modal, and the pencil/CTA are hidden entirely (not just disabled) rather than leaving dead click targets on a card you might be about to discard.
- **Collect button pulse + real double-submit fix**: a small green dot renders inside "🔍 실시간 수집" only while `activeJob` is truthy — conditionally *rendered*, not just hidden, so the pulse animation genuinely doesn't run (or cost anything) while idle. `disabled={!!activeJob}` on the button, but the actual guard lives in the click handler itself (checked before calling `collect()`), so a stray Enter keypress in the query input — the exact double-submit race an earlier session's testing flagged but left unfixed — can't slip a second job past it either. Verified live: button `disabled: true` and dot present/animating (`getComputedStyle` confirmed `animation-name: collect-pulse`, color exactly `var(--green)`) in the same tick as the click; both cleared once the job finished.

## 2026-07-25 — Delete vs revert on discard; Studio Step 2 visual redesign (Claude Code)
- **Collected-results tab now only shows new/updated ads as review cards** — unchanged ones are noise once you're specifically reviewing what a re-scrape found. `AdsContext.collect()` filters `collected` to exclude `status === 'unchanged'` before setting it, but still needs to report that count in the summary line, so it's carried separately as `collectedUnchangedCount` (sourced from `job.summary.unchanged`) rather than derived from `collected`, which no longer has any.
- **Discard now distinguishes delete from revert**, since by review time the sheet has already been overwritten with the new scrape's values — undoing a `'new'` ad and undoing an `'updated'` one are fundamentally different operations:
  - `upsertAdRows` (sheets.service.js) now captures `previousValues` (the full old row, padded to all 22 columns — Sheets omits trailing empty cells from returned rows, and a short array here would've left stale trailing columns un-reverted) on every `'updated'` status entry, since that's the only way to get back to the pre-scrape state once the row's already been overwritten.
  - New `revertAdRow(adArchiveId, previousValues)` — same row-finding mechanism as `updateAdField`, just overwrites the whole `A:V` range instead of one cell.
  - `POST /api/ads/discard`'s shape changed from `{ keyword, adArchiveIds }` to `{ keyword, items: [{adArchiveId, action: 'delete'|'revert', previousValues}] }`. `'delete'` items keep the existing behavior (Drive trash + row delete); `'revert'` items call `revertAdRow` and **deliberately leave Drive media alone** — noted as a simplification, not solved: any files this scrape uploaded for an updated ad become harmless orphans once the row points back at the old (still-valid) Archived Image Links. Each item's failure is still independently collected, same as before.
  - Frontend: `discardAds()` now takes full ad objects (not bare IDs) since it needs each one's `status` and, for reverts, `previousValues` — both now carried on every collected ad via `collect()`'s status lookup, same place `changedFields` already came from. After a successful call it re-fetches `ads` from the backend rather than hand-simulating the revert locally (simpler and can't drift from what the sheet actually has). `CollectedResults.jsx` shows a short helper line while in select mode ("신규 항목은 해제 시 삭제되고, 업데이트 항목은 해제 시 이전 정보로 복원됩니다") since unchecking now means two different things depending on the ad's status.
  - **Verified all four paths live, not just the toast**: re-collected a never-before-seen keyword (`탈모`) to get guaranteed `'new'` ads, unchecked one, confirmed via direct API/Drive checks that its row was genuinely gone and its file trashed. For the revert path, manually dirtied one archived ad's `CTA Text` via a raw script call (simulating "this field drifted"), re-collected its keyword so the real scrape overwrote it back and the ad landed as `'updated'` with `CTA Text` in `changedFields`, unchecked it, and confirmed the row **still exists** (not deleted) with `CTA Text` restored to the dirtied test value — and that its Drive file was completely untouched throughout. Cleaned up afterward: restored that ad's `CTA Text` to its real scraped value ("See details") so the test artifact didn't linger in the client's actual sheet.
- **Studio Step 2 (`StepReferenceAds.jsx`) redesigned to match the Feed screen's visual standard.** It was still rendering the original mockup's placeholder look — a 150px fixed-width horizontal row of gradient boxes with cramped overlaid text — completely inconsistent with the real-photo card treatment already built for the feed. Now: a responsive CSS grid (`.refrow`, `auto-fill`/`minmax(180px,1fr)`, near-identical to the feed's own `.grid`) of cards reusing `Thumb`/`RetryImage` for real photos (gradient fallback only when an ad genuinely has none, same convention everywhere else), the shared `Badge` component for corner status/media badges, and a truncated one-line copy snippet below the photo instead of overlaid text. Selection stays plain click-to-toggle multi-select — this step doesn't get the feed's separate "select mode" toggle, since every card here is always selectable by design. New "상세보기" link (`stopPropagation`) opens `AdDetailModal` — reused exactly as-is rather than rebuilt, per the plan — so the user can check an ad's full record without losing their in-progress selection; its footer's "이 광고로 생성하기" was deliberately left untouched rather than hidden/relabeled (a judgment call, sanctioned by the task) since clicking it just collapses the wizard's selection to that one ad, a defensible shortcut rather than a bug. Copyright-safety hint text about layout/color/copy-structure-only analysis kept verbatim, unchanged. Empty state (0 archived ads for the brand) gets a friendly sentence instead of a blank grid.
  - Verified live: picked a brand with 19 archived ads, confirmed all 19 rendered as real photos in a genuine CSS grid (`display: grid`) with loaded images (`naturalWidth > 0` for all), multi-selected two cards, opened "상세보기" on a third (confirmed the modal showed the right ad and the two-card selection was untouched both while the modal was open and after closing it), then advanced to Step 3 to confirm the wizard still flows end-to-end. Zero console errors across the whole session.

## 2026-07-25 — Studio Step 3: Cafe24 + Pinecone product sync (Claude Code)
- **Built the full pipeline**: Cafe24 (per-mall OAuth, `fetchAllProducts`) → GPT-4o-mini analysis (`analyzeProduct.service.js`, 7 fixed Korean fields, Pinecone few-shot lookup, verbatim-carried anti-hallucination instruction — "없음" instead of inventing promo/authority info) → `text-embedding-3-small` embedding → Pinecone upsert (namespace per brand) → new `제품` tab in the existing Google Sheet. `productSync.service.js` orchestrates it as an in-memory job (same `startX()`/`jobId`/poll pattern as `collect.service.js`), with per-product failures collected into `job.summary.failures[]` rather than aborting the run (same resilience pattern as `discardAds`).
- **One deliberate deviation from the literal instruction, flagged up front**: told to validate the new Cafe24/OpenAI/Pinecone vars "fail-fast, same pattern as the Google vars" — but doing that literally would mean the *entire server*, including the already-working ad-collection/Sheets/Drive features, refuses to boot until real Cafe24/OpenAI/Pinecone credentials exist, which don't yet. Instead, `config/index.js` validates the whole group together: if none of the ~11 vars are set, product sync is simply reported unconfigured (`config.productSyncConfigured = false`, `/api/products/*` routes return `503`) and the rest of the server boots normally; if *any* are set, *all* must be, and that failure is still fail-fast at startup, same as everything else in the file. Verified live: booted the server with zero product-sync vars in `.env` — health check OK, `GET /api/products` returned `[]` (auto-creating the `제품` tab + header row on first read), `POST /api/products/sync` returned a clean `503`, and the pre-existing ad-collection routes were unaffected.
- **Cafe24 tokens needed a fundamentally different architecture than Google's**: access tokens expire in ~2h and refresh tokens themselves rotate (and expire in ~2wk) on every use, unlike the one static refresh token pasted into `.env` for Sheets/Drive. New `cafe24.client.js` persists the current pair per brand in a gitignored, mutable `backend/data/cafe24-tokens.json`, refreshing-and-rewriting automatically when close to expiry. New `scripts/cafe24-auth.js` (mirrors `scripts/google-auth.js`'s local-callback-server pattern) does the one-time per-mall consent flow to seed that file.
- **Pinecone diffing instead of the n8n workflow's `clearNamespace` wipe-and-rebuild**: `deleteStale()` lists each brand's namespace and removes only vector IDs no longer in the mall's current catalog — same non-destructive philosophy as `reset-archive.js` being explicit/manual and `upsertAdRows`'s diff-not-overwrite. Deliberately scoped to the *current catalog* (every fetched product ID), not just this run's *successfully analyzed* ones — a product that merely hit a transient GPT/embedding error this run keeps its existing vector from a prior successful sync rather than losing it.
- **Other judgment calls, all documented in README.md**: product rows live in a new `제품` tab in the *same* spreadsheet (not a second spreadsheet) — reuses the existing `SHEET_ID`/auth, auto-created with its header row on first use. `toggleMyBrand` kept its original index-based signature (brand list is now a fixed 2-entry array from `ProductsContext`, so index-based still just works). `ProductField.jsx` deleted (dropdowns are gone — price/promo/ad-hook-copy are now single real values, not option arrays to choose between); `initialBrands.js` left in place but unused, with a one-line comment marking it dead/reference-only.
- **Frontend**: new `ProductsContext.jsx` (mirrors `AdsContext`'s load-on-mount + job-polling shape), nested before `StudioProvider` in `AppProviders.jsx`. `StudioContext.jsx` now derives `myBrands` from `useProducts()` instead of the `initialMyBrands` mock; `selections` simplified from `{product, priceIdx, promoIdx, msgIdx}` to just `{product}`, computed fresh each render as `override ?? defaultSelectionFor(brand)` rather than stored eagerly — a stale pick (a product name that dropped out of a resync) self-heals back to the first available product instead of pointing at nothing. `StepMyBrand.jsx` rewritten: three read-only fields (Price/Promotion/광고 후킹 카피, labeled "● 자사몰 API") replace the old option-dropdown UI, plus a per-brand "제품 동기화" button and `SyncProgress.jsx` (new, scoped `.sync-*` CSS variant of `feed.css`'s `.cp-*` classes rather than a cross-import — same pattern as `.refrow` mirroring `.grid`).
- **What's code-complete vs. blocked on real credentials**: every backend service, mapper, controller/route, and the whole frontend layer is written, unit-tested (9/9 `node --test` passing, including new `product.mapper.test.js`), and verified to boot/build/run cleanly. What's **blocked** on real Cafe24 app registration + OpenAI + Pinecone account credentials (none exist yet): actually running a sync job end-to-end (fetching real products, GPT analysis, embeddings, Pinecone upsert) and the one-time `cafe24-auth.js` consent flow per mall. Verified live in the browser instead: selecting/deselecting both brands on Step 3 works, the sync button correctly starts a job and surfaces the expected `503 Product sync is not configured` as a toast (no crash), and Step 4's "생성 시작" still queues gallery cards correctly even with zero synced products (falls back to just the brand name). One side effect worth noting: this live testing did create the real `제품` tab (header row only, no data) in the client's actual Google Sheet, since that part of the pipeline only needs the already-configured Sheets credentials.
- **Unrelated bug noticed in passing, not fixed here** (flagged as a separate background task): `GalleryContext.jsx`'s `addResult()` module-level `nextId` counter can collide with `initialGallery.js`'s own mock IDs after an HMR reload, producing a React duplicate-key warning in `GalleryScreen`. Nothing to do with this session's changes — didn't touch `GalleryContext.jsx`.

## 2026-07-25 — 상품 관리 (Product Management) screen (Claude Code)
- **New top-level screen for Cafe24/Pinecone connection status + browsing synced products**, replacing the mock "브랜드·자사몰 연동" section in `SettingsScreen.jsx` that only ever showed `initialProductReview.js` fake data behind a `window.prompt()` "수정" button that never persisted anywhere real.
- **Backend**: `pinecone.service.js` gained `getNamespaceStats(brandKey)` (`describeIndexStats()`, try/catch → `{ vectorCount: null }` — a status display should never 500 just because the index doesn't exist yet) and `resetNamespace(brandKey)` (`deleteAll()` — destructive, manual-only, same precedent as `scripts/reset-archive.js`). `cafe24.client.js` gained `isAuthorized(brandKey)` (reads the token store, no network refresh — status-display only). New `GET /api/products/status` (always `200`, `{ brands: [], productSyncConfigured: false }` when nothing's configured rather than a `503` — this endpoint's whole job is reporting that state, not gatekeeping behind it) and `DELETE /api/products/:brand/pinecone` (same `503`/`404` guards as `postProductSync`).
- **Frontend**: `ProductsContext.jsx` gained `status` (loaded on mount, re-fetched after every successful `sync()`) and `resetNamespace(brandKey)`. New `src/components/products/`: `ProductsScreen.jsx` (per-brand `BrandConnectionCard`s, or one explanatory empty-state card when `!productSyncConfigured`), `BrandConnectionCard.jsx` (3-state Cafe24 status pill — 연결됨/인증 필요/설정 필요 — product/vector counts, inline `SyncProgress` reuse, and a collapsed `<details>` "고급" section with a type-the-brand-name-to-confirm destructive Pinecone reset — same high-friction precedent as `reset-archive.js`), `ProductBrowser.jsx` + `ProductRow.jsx` (search + brand-chip filter over collapsible read-only rows, adapted from the old `ProductReviewPanel` shape minus the review badge/edit button). New nav entry in `Sidebar.jsx` between 피드 and 스튜디오 (pipeline order: gather → manage own products → generate → review → settings). New `src/styles/products.css`, importing `settings.css` alongside it for the shared `.set-sect`/`.row`/`.prod-review`/`.pr-*` classes rather than duplicating them.
- **Judgment call: deleted rather than commented-out the retired mock code.** `BrandSyncRow.jsx`, `ProductReviewPanel.jsx`, `ProductReviewField.jsx`, and `initialProductReview.js` were the task's explicit "your call" list; since `SettingsContext.jsx` had zero remaining consumers once those were gone (it existed only to serve that mock section), deleted it too and removed `SettingsProvider` from `AppProviders.jsx` — leaving a fully-orphaned Context wired into the provider tree seemed worse than the "your call" scope technically covered. `SettingsScreen.jsx` now only renders `N8nIntegrationCard`, untouched per the task.
- **Verified live**: rebuilt (`npm run build`, clean), restarted both dev servers, and drove the new screen in a browser — `상품 관리` nav item present, unconfigured state shows the single explanatory card (not broken per-brand cards), `ProductBrowser`'s brand chips (전체/헬시키키/키키뷰티) and empty-state message render correctly, `설정` screen shows only the n8n card with no leftover mock section, and Studio Step 3 still works normally (confirming removing `SettingsContext`/`SettingsProvider` didn't break anything). Backend: `npm test` still 9/9, and `GET /api/products/status` / `DELETE /api/products/:brand/pinecone` both verified to degrade gracefully (`200 {brands:[], productSyncConfigured:false}` / `503`) with product sync left unconfigured.
- **Still blocked on real credentials** (same as the prior entry): the 3-state Cafe24 pill will only ever show 연결됨/인증 필요 once real Cafe24 apps are registered and `cafe24-auth.js` has been run; Pinecone vector counts will read `0`/`—` until a real index + sync exist. The destructive namespace-reset action couldn't be live-tested end-to-end for the same reason, but its guard logic (503/404/type-to-confirm) was verified by inspection and the same request-level checks reused from `postProductSync`.

## 2026-07-26 — Cafe24 redirect-URI fix; deployed and ready for real auth (Claude Code)
- **Root cause**: the original `scripts/cafe24-auth.js` (mirroring `google-auth.js`) tried to spin up a local `http://localhost:3002/oauth2callback` server for the OAuth redirect. Cafe24's app-registration console rejects `localhost` outright with "domain 형식이 올바르지 않습니다" — unlike Google, it requires a real, publicly resolvable domain for the redirect URI, not just any valid-looking URL. A local callback server was never going to work here, no matter the port.
- **Fix: redirect to a real deployed page instead of a local server.** New `src/pages/Cafe24CallbackPage.jsx` — a standalone route (no Sidebar, no Context providers, no `/api` calls) whose only job is reading the `code`/`state`/`error` query params Cafe24 hands back and displaying the next command to run. `src/main.jsx` now branches on `window.location.pathname === '/cafe24-callback'` before mounting the main `<App/>`, and new `vercel.json` adds the SPA rewrite (`/(.*) -> /index.html`) so a direct browser hit on that path doesn't 404 against Vercel's static host — client-side routing needs that rewrite to work at all for a path with no matching static file.
- **The one-time auth script split into two steps** since there's no longer a local server to catch the redirect and finish the exchange automatically:
  - `scripts/cafe24-auth.js <brandKey>` (rewritten) — just prints the authorize URL. No local server, no `selfsigned` dependency (removed from `backend/package.json` entirely).
  - `scripts/cafe24-auth-exchange.js <brandKey> <code>` (new) — step 2, run manually after copying the code+command shown on `Cafe24CallbackPage`. Exchanges the code for tokens via the existing `cafe24.client.js` `exchangeCodeForTokens()`, saves to `backend/data/cafe24-tokens.json`. The client secret and the actual token exchange still never leave the developer's machine — only the authorization *code* (short-lived, useless without the secret) touches the deployed page.
- **`backend/src/config/index.js` validation loosened from all-or-nothing to per-brand**: previously required *both* brands' Cafe24 vars plus OpenAI/Pinecone all set together before `productSyncConfigured` could be true. Real onboarding happens one mall at a time (헬시키키 first), so each brand's 3 `CAFE24_*` vars now validate as their own independent group (unset entirely → brand just isn't onboarded yet, not an error; partially set → fail-fast, since that's always a mistake) — OpenAI/Pinecone are only required once *at least one* brand is configured. Also added `cafe24RedirectUri` (defaults to `https://adgen-studio-red.vercel.app/cafe24-callback`, overridable via `CAFE24_REDIRECT_URI`).
- **Verified before deploying**: `npm install` in both the frontend root and `backend/` to sync lockfiles (frontend lockfile had 46 stale/extraneous entries pruned — `package.json` itself didn't change; backend lockfile lost the `selfsigned` entry as expected). `npm run build` (frontend) clean, `node --test` (backend) still 9/9.
- **Deployed**: committed and pushed to `main`, confirmed the Vercel deployment went `READY`, then fetched `https://adgen-studio-red.vercel.app/cafe24-callback` with no query params and confirmed it renders "잘못된 접근" (the callback page's no-code state) rather than 404ing — the SPA rewrite and the new route are both live in production.
- **What's still manual, on purpose**: actually running `cafe24-auth.js`, opening the authorize URL, and logging into the 헬시키키 mall admin account to approve the consent screen all require a human with those Cafe24 credentials — none of that was attempted here, per explicit instruction. Once jaehyuk has registered `https://adgen-studio-red.vercel.app/cafe24-callback` as the app's redirect URI in the Cafe24 console and completed that approval (producing a real `backend/data/cafe24-tokens.json`), the actual end-to-end verification — running `POST /api/products/sync` for real and confirming rows land in the Sheet's 제품 tab and vectors land in Pinecone's `adgen-products` index — is the next follow-up, not part of this task.

## 2026-07-26 — First real product sync (healthykiki): two field-mapping bugs found and fixed (Claude Code)
- **jaehyuk completed the manual Cafe24 OAuth consent** (logged into the 헬시키키 mall admin, approved on the deployed callback page). First attempt's authorization code expired before `cafe24-auth-exchange.js` could run (Cafe24 codes are short-lived, minutes not hours) — no partial state was written, just re-ran the authorize step for a fresh code, which exchanged successfully. `backend/data/cafe24-tokens.json` now holds a real `healthykiki` entry (accessToken/refreshToken present, expiresAt ~2h out, refreshExpiresAt ~2wk out — both matching Cafe24's documented lifetimes, confirming the token shape is correct without ever printing the values).
- **This was the first time real Cafe24 data ever hit `analyzeProduct.service.js`/`cafe24.service.js`/`pinecone.service.js`, and it surfaced two real bugs** — both fixed rather than papered over, per instruction:
  1. **Cafe24's product LIST endpoint never returns `description`/`simple_description`/`mobile_description`, no matter what's requested via `fields`** — confirmed empirically (a `fields=...,description,...` request on the list endpoint silently omits those keys entirely). Only the per-product **detail** endpoint (`GET /admin/products/{product_no}`) returns them, and it's otherwise a superset of the list item's fields. Fixed `cafe24.service.js`'s `fetchAllProducts` to fetch each product's full detail individually (bounded concurrency of 3, same pattern as `collect.service.js`'s media downloads) after paging through the lightweight list — so `analyzeProduct.service.js`'s existing `rawProduct.description`/`simple_description` reads now actually get real content instead of silently analyzing off `summary_description` alone.
  2. **Pinecone rejects vectors whose metadata exceeds 40KB** — the very first real sync attempt (4 products) came back `synced: 3, failed: 1` with `"Metadata size is 41116 bytes, which exceeds the limit of 40960 bytes per vector"`. Root cause: `productSync.service.js` was storing the *entire raw Cafe24 product JSON* (`rawData: JSON.stringify(rawProduct)`) in Pinecone metadata, and real product detail pages carry 10-25KB+ of "edibot" page-builder HTML in `description`/`mobile_description` — comfortably enough on its own to blow the cap once the per-product detail fetch (fix #1) started actually populating those fields. Checked whether anything reads `metadata.rawData` back anywhere in the codebase first — nothing does, only `aiAnalysis` is ever parsed (by `queryFewShot`'s few-shot lookup) — so the fix was to drop `rawData` from the metadata entirely rather than truncate it, since keeping any version of genuinely-unused data was the actual problem.
- **Re-ran after both fixes: 4/4 products synced, 0 failed, 0 stale.** Full job summary: `{totalProducts: 4, synced: 4, failed: 0, staleDeleted: 0, failures: []}`.
- **Verified all the way through, not just the job summary**: `GET /api/products?brand=healthykiki` returns 4 real rows (real Cafe24 product names/prices/image URLs, e.g. "[🔐비밀링크 할인 39%] 컨투올잇" at 49800.00), confirming the Sheet's 제품 tab has genuine data since that endpoint reads directly from it. `GET /api/products/status` shows `authorized: true`, `productCount: 4`, `pinecone: {vectorCount: 4}`. Checked the 상품 관리 screen in a real browser: 헬시키키's card shows "● 연결됨", "제품 4개 · 마지막 동기화 [real timestamp]", "Pinecone 벡터 4개", and the product browser lists all 4 real products with their expanded GPT-generated fields.
- **Sanity-checked the GPT output for sense, not just non-emptiness**: all 4 rows are variants of the same underlying skincare "contouring" product (콘투올잇/Contoul-it) listed under different promo links, so near-identical 제품특성/효과효능/페인포인트/권위신뢰 across them is expected, not a bug. Anti-hallucination held up in practice: the 2 listings whose titles carry an explicit discount ("[한정판매 비밀링크 할인 51%]", "[🔐비밀링크 할인 39%]") got that exact text in 프로모션 정보, while the 2 listings without a discount in their title correctly got `없음` rather than an invented promotion.
- **키키뷰티 deliberately untouched** — still zero Cafe24 vars set for it, `config.brands` correctly contains only `healthykiki`, and the product-management screen correctly shows only the one brand card. Per-brand onboarding (added in the redirect-URI-fix session) working as designed.
- **Not yet committed**: the two bug fixes (`cafe24.service.js`, `productSync.service.js`) are sitting uncommitted alongside real test data now live in the client's actual Google Sheet/Pinecone index — this entry documents what changed and why, but committing/pushing wasn't part of this task.

## 2026-07-26 — Real product photos + formatted prices in the product UI (Claude Code)
- **Committed and pushed the two prior bug fixes** (`cafe24.service.js` detail-hydration, `productSync.service.js` metadata trim) plus their PROGRESS.md entry, as their own isolated commit — nothing else bundled in.
- **Investigated Cafe24's real product detail response for a genuine photo gallery, empirically rather than guessing**: no `additional_image`/`sub_images`/`images` field exists (explicitly requesting them via `fields` on the live API silently omits them, same as the earlier `description` finding), and `GET /admin/products/{no}/images` 404s "No API found". Cross-checked all 4 real healthykiki products — each only ever exposes the same 4 known fields (`detail_image`/`list_image`/`small_image`/`tiny_image`), which are differently-sized renditions of **one** photo, not separate gallery images. Conclusion: there is no multi-photo gallery available from this API for this mall right now.
- **`product.mapper.js`'s `Image URL` column redesigned around that finding**: newline-joined multi-URL shape (matching `ad.mapper.js`'s `Image Links` convention, capped at 20) so nothing needs to change if Cafe24 ever exposes real additional photos — but today it always resolves to exactly one URL (the best-quality `detail_image`, falling back through `list_image`/`small_image`/`tiny_image`), never listing the same photo twice under a misleading "이미지 여러 장" count. Added a dedicated test proving all-4-fields-present still yields one URL, not four. This is a real, deliberate column-shape change to a column that already has a live header + 4 real rows in the client's Sheet — chosen specifically because it requires no migration (old single-URL rows are just a length-1 "array").
- **Confirmed Cafe24 image URLs are plain public CDN links, unlike Facebook's signed/expiring ones**: `curl -I` against a real `ecimg.cafe24img.com` URL came back `200` with no `Authorization` header sent and a real `Cache-Control: max-age=86400` — directly `<img src>`-able, no Drive-style re-hosting/archiving step needed for products.
- **New `src/utils/currency.js`** (`formatKRW`) and **`src/api/adaptProduct.js`** (mirrors `adaptAd.js` — splits the newline-joined `Image URL` into `images[]`/`primaryImage`, applies `formatKRW` to price). `ProductsContext.jsx`'s `groupByBrand` now sources from `adaptProduct`'s output instead of raw sheet rows, so Studio Step 3's price field got formatted "for free" — confirmed `StepMyBrand.jsx` needed zero code change for this, just a clarifying comment, since the formatting already flows through the shared adapter.
- **`ProductBrowser.jsx` rewritten from an accordion list into a real-photo grid**: new `ProductCard.jsx` (own scoped `.prod-grid`/`.prod-card` CSS variant of studio.css's `.refrow`/`.refpick` pattern, brand-color dot, formatted price, an "이미지 N장" badge that only ever shows when `images.length > 1` — which in practice is never, per the gallery finding above, so no card currently shows a misleading count) and `ProductDetailModal.jsx` (structured like `AdDetailModal.jsx`: image gallery grid + the exact same `ImageLightbox` component reused as-is, then 기본 정보 and AI 분석 sections with 프로모션/광고 후킹 카피 visually emphasized first). `ProductRow.jsx` deleted outright (direct architectural replacement, not dead code to leave behind).
- **Re-ran the real healthykiki sync end-to-end with all of the above live**: `{totalProducts: 4, synced: 4, failed: 0, staleDeleted: 0}` again. Verified beyond the job summary: `GET /api/products?brand=healthykiki` shows each row's `Image URL` is now the single `detail_image` URL (e.g. `.../product/big/20260715/279255f6....png`), not the old `list_image`; the raw `Price` cell correctly stays an unformatted Cafe24 string (`"56000.00"`) since formatting is a frontend-only concern. In the browser: the product grid shows real formatted prices (49,800원/39,800원/56,000원/49,800원) with zero misleading image-count badges, clicking a card opens the detail modal with the right 기본 정보/AI 분석 content, and a direct `naturalWidth`/`curl -I` check confirmed all 4 real Cafe24 photos are genuinely loadable (not broken links) independent of the browser's own lazy-loading timing.
- **Known, deliberately-accepted cosmetic mismatch**: `ImageLightbox`'s "원본 파일 열기 (Drive)" link label is Drive-specific wording, reused as-is per instruction for product photos too (which aren't Drive files) — flagged as a background task for a later small polish pass rather than modifying a shared component out of scope here.
- **키키뷰티 still untouched** throughout this session, as instructed.

## 2026-07-26 — Pure refactor: eliminate verified duplication, zero behavior change (Claude Code)
- **Scope discipline**: this was a pure refactor per explicit instruction — no feature/UI changes. After every extraction below, ran the full backend test suite and smoke-tested every affected screen (Feed collection, Studio Step 2/3, 상품관리's sync + detail modal, Feed's ad detail modal) before moving to the next item, exactly as asked.
- **1. `backend/src/mappers/pickField.js`** — the byte-identical `pick(obj, keys)` helper duplicated in `ad.mapper.js` and `product.mapper.js` now lives in one file, imported by both. `node --test`: 10/10 unchanged.
- **2. `src/utils/date.js`** (`formatDateTime(iso, { fallback, dateStyle, timeStyle })`) replaces 4 scattered near-identical copies (`StepMyBrand.jsx`, `BrandConnectionCard.jsx`, `AdDetailModal.jsx`, `ProductDetailModal.jsx`). The two pairs actually had different defaults — `StepMyBrand`/`BrandConnectionCard` used `dateStyle:'medium',timeStyle:'short'` with fallback `null`/`'없음'` respectively; `AdDetailModal`/`ProductDetailModal` used plain `toLocaleString('ko-KR')` (no style object — full format incl. seconds) with fallback `'—'`. Made `dateStyle`/`timeStyle` genuinely optional (only passed to `toLocaleString` when at least one is set) so the two detail modals keep their exact original format instead of silently picking up medium/short. Verified in a live browser by reading each of the 4 rendered strings before/after: `2026. 7. 26. 오전 2:07:36` (product detail, full format, unchanged) vs. `2026. 7. 26. 오전 2:07` (product-management card, medium/short, unchanged) vs. `2026. 7. 24. 오후 11:27:30` (ad detail, full format, unchanged).
- **3. `src/hooks/useJobPolling.js`** replaces the copy-pasted start/poll/toast/finally skeleton in `AdsContext.collect()` and `ProductsContext.sync()`. Kept genuinely different behavior out of the shared hook: `collect()`'s `RATE_LIMITED`-specific error message stays in its own `onFailed`; `ProductsContext`'s `getStatus` merges `brandKey` back into each poll response (not part of the job API's own shape) since the hook itself stays domain-agnostic. Each context still owns its poll interval (900ms ads / 1200ms products) via the hook's constructor option. **Live-verified with real jobs, not just a build check**: ran a real product sync (4/4 synced, live progress UI updated correctly) and a real ~10s Apify collection (real ad archived, live "검색 중..." → done toast). The test collection surfaced a **pre-existing, unrelated edge case** — an Apify result with an empty `ad_archive_id` that the discard flow can't remove via the UI (flagged separately as a background task) — cleaned up manually via a direct Sheets API script matching on Search Keyword, confirmed archive count back to 167 afterward.
- **4. `src/components/common/DetailField.jsx`** (union of both call sites' props: `label`/`value`/`href`/`emphasized`) and **`src/hooks/useGalleryLightbox.js`** (lightbox-index state + the two-stage Escape/backdrop-close behavior) replace each detail modal's hand-rolled copies. **Verified the exact two-stage behavior by hand for both modals**: opened each detail modal, opened its lightbox, dispatched one Escape (lightbox closed, modal stayed open) then a second (modal closed) — confirmed via direct DOM queries after each keypress, for both the ad and product detail modals.
- **5. `backend/src/services/sheetsBase.js`** (`getClient()`/`callSheets()`/`tabRange`-builder) extracted from `sheets.service.js` and `productSheets.service.js` — each file's own gid-lookup/tab-creation helpers and all upsert/diff business logic stayed exactly where they were; only the client/retry/range-building scaffolding underneath moved. This touches the two most critical persistence paths in the app, so beyond `node --test` (10/10 unchanged), did the live check explicitly asked for: captured `GET /api/ads` and `GET /api/products?brand=healthykiki` responses before the change, restarted the backend with the refactor applied, fetched both again, and `diff`'d — **byte-identical** both times.
- **6. `backend/src/utils/jobStore.js`** (`createJobStore()` — `Map` storage, `randomUUID()`-based job creation, the fire-and-forget catch-and-mark-failed mechanics, `getJob(id)`) extracted from `collect.service.js` and `productSync.service.js`; each keeps its own store instance (separate `Map`s), progress/summary shape, `runJob` logic, and failure-handling callback (collect's `RATE_LIMITED` branch stays put). Live-verified per instruction: ran one real ad collection and one real product sync end-to-end afterward through the actual `POST`/poll endpoints — both reported `done` with correct summaries, exactly as before. Cleaned up the test collection's artifact row afterward the same way as in item 3.
- **Net effect**: two backend files (`ad.mapper.js`, `product.mapper.js`) plus six more (`sheets.service.js`, `productSheets.service.js`, `collect.service.js`, `productSync.service.js`) lost their duplicated scaffolding; four frontend files (`StepMyBrand.jsx`, `BrandConnectionCard.jsx`, `AdDetailModal.jsx`, `ProductDetailModal.jsx`) lost their duplicated date/Field/lightbox logic; `AdsContext.jsx`/`ProductsContext.jsx` lost their duplicated polling loop. Zero behavior change confirmed at every step — tests, byte-diffs, and hands-on browser checks, not just "it still builds."

## 2026-07-26 — Two background-task fixes: ImageLightbox label, undiscardable empty-ID ads (Claude Code)
- **`ImageLightbox.jsx`'s "원본 파일 열기 (Drive)" label generalized to "원본 이미지 열기"** — it's reused as-is by `ProductDetailModal.jsx` for Cafe24 CDN photos, which aren't Drive files, so the old label was technically wrong there (functionally harmless, just misleading text). `toEmbeddableImageUrl` itself stayed untouched (it already no-ops for non-Drive URLs); only the label and the component's top comment changed to reflect it genuinely serves both callers now. Verified in the browser for both the ad detail modal and the product detail modal.
- **Ads with an empty Ad Archive ID (a rare malformed Apify result) can no longer get stuck in the sheet undiscardable.** Root cause: such an ad still got archived via `upsertAdRows`, but the discard flow — and `upsertAdRows`' own dedupe — both match rows by Ad Archive ID, so a genuinely empty ID can never be targeted; the only way to remove one was a manual direct Sheets API script (done twice this session for test artifacts). Fixed at the ingestion step per the flagged task's own recommendation (option (a), "more robust since an ad with no archive ID isn't a useful record anyway"): `collect.service.js`'s `runJob` now filters `mappedAll` down to ads with a real Ad Archive ID *before* media-archiving/upserting, logging `skipped N scraped item(s) with no Ad Archive ID for keyword "..."` for visibility rather than silently dropping them. `totalAdsFound`/`totalAds`/`perKeyword.ads` still reflect the raw scrape count (transparency about what Apify actually returned); only the archived/persisted/UI-displayed set is filtered. **Verified live**: re-ran a real collection with a keyword known to trigger this Apify quirk — job reported `totalAdsFound: 1` but `appended: 0`/`recentItems: []`, the archive count stayed at 167 (nothing landed in the sheet this time), and the expected warning appeared in the server log.

## 2026-07-26 — Product reference-image extraction (Part B) + Step 3 UI (Part C) (Claude Code)
- **Part A**: `Thumb.jsx` gained a `fit` prop (`'cover'` default, unchanged for every existing ad/feed usage). `ProductCard.jsx` now passes `fit="contain"` — Cafe24 product photos are tall bottle/tube shots that `cover` was cropping badly. Also found and fixed a genuine pre-existing gap while in there: `.prod-card .thumb` had no `position/overflow` and `.thumb-img` had no sizing rule at all in `products.css` (not just "wrong fit value" — the rule was simply missing since `ProductCard.jsx` was first built).
- **Part B — product reference-image extraction, new end-to-end feature**: every product only had Cafe24's raw marketing photo (may include hands/staging); the future ad-generation pipeline needs a clean isolated product cutout to composite reliably.
  - `product.mapper.js`: `PRODUCT_COLUMNS` split into `SYNC_COLUMNS` (12, sync-job-owned) + `EXTRACTION_COLUMNS` (`Extracted Image URL`, `Extracted At` — appended, owned only by the new extraction path). `mapProduct()` never produces these keys, and `upsertProductRows`'s existing-row updates are restricted to `A..SYNC_LAST_COLUMN` so a resync can never silently blank out extraction results.
  - `productSheets.service.js`: `ensureProductTab()` now migrates an existing-but-shorter header in place (the live sheet already had 12 columns + 4 real rows) rather than just skip-if-exists; `LAST_COLUMN` bumped to `'N'`; new `updateProductField(productId, columnName, value)` mirrors `sheets.service.js`'s `updateAdField`. **Live-migrated the real sheet** — confirmed header is now exactly the 14 expected columns, the 4 existing rows' data untouched.
  - New `imageIO.service.js`: `downloadImageAsBase64(source)` (Drive `webViewLink` via `files.get(alt:'media')` for original bytes, or a plain public URL via `fetch`) and a generic `uploadImage(base64, {rootFolderName, subfolder, fileName})`, generalized from `drive.service.js`'s folder/upload/permission pattern so both this feature's "AdGen Product References" root and the future ad-generation pipeline's "AdGen Generated Ads" root share the same upload mechanics without duplicating it.
  - New `productImageExtraction.service.js`: `extractProductImage(brandKey, productId)` — downloads the raw photo, calls OpenAI's Responses API `image_generation` tool in edit mode with a from-scratch prompt (no n8n precedent existed for this step) to isolate the product on a plain white background, uploads the PNG result, writes both new columns back via `updateProductField`. Synchronous request/response by design (one paid call per use, not a job).
  - New `POST /api/products/:brand/:productId/extract-image` (controller + route), gated the same way as other product-sync endpoints (503 if unconfigured, 404 for unknown brand/product).
  - Frontend: `adaptProduct.js` gained `extractedImage`/`extractedAt` (and a bug caught during Part C's live check: the stored value is a Drive `webViewLink`, same as archived ad media — needed the same `toEmbeddableImageUrl` conversion `adaptAd.js` already does, which was missing initially and would have left every extracted-image `<img>` broken); `backendClient.js` gained `extractProductImage`; `ProductDetailModal.jsx` gained a "우리 제품 참조 이미지" section (extract/재추출 button with loading state via `ProductsContext`'s new `extractImage`/`extractingIds`); `ProductCard.jsx` gained a "레퍼런스 준비됨" badge.
  - **Model note**: `gpt-4.1` and `gpt-4o-mini` as the Responses API orchestrator model both hit a 403 ("organization must be verified") on this account — initially suspected as an OpenAI org-verification gate (a plain chat-completion call with the same model/key succeeded, confirming the model itself wasn't the issue), but switching the orchestrator to `gpt-5.5` resolved it on the very next real call without any org-verification changes. Root cause not fully pinned down given the conflicting evidence, but `gpt-5.5` is confirmed working and is what's shipped.
  - **Verified with exactly one real paid call** (healthykiki product 11, "컨투올잇 Contoul-it"): HTTP 200, Drive link returned, sheet's two new columns populated correctly, and the downloaded PNG visually confirmed as a genuinely clean cutout (box + bottle isolated on plain white, no hands/staging, all Korean label text preserved accurately).
- **Part C — Studio Step 3 (`StepMyBrand.jsx`) brought in line with 상품관리's card/image language**, structure unchanged (brand list, then per-brand product-config panel):
  - Plain `<select>` product picker replaced with clickable product cards (thumbnail + name + price, "레퍼런스 준비됨" badge), reusing `products.css`'s `.prod-card`/`.prod-grid` classes directly (this is one bundled SPA — its CSS is already global regardless of which screen's JS imports the file) rather than duplicating card markup; falls back to the native `<select>` at 8+ products. New `.prod-card.selected`/`.prod-card.static` modifiers added to `studio.css`.
  - New reference-image row shows the original marketing photo and the extracted image (or a "상품관리에서 제품 참조 이미지를 추출하세요" note if none exists yet) side by side — extraction itself stays centralized in 상품관리, no duplicate button here.
  - `ProductsContext.jsx`'s `groupByBrand` extended to carry `extractedImage`/`extractedAt` through to Step 3's per-name product dict (previously only had price/promo/copy/imageUrl).
  - **Live-verified against real healthykiki data** in the browser: product cards render with correct thumbnails/prices, the badge lands on the correct (and only the correct) extracted product, selecting a card correctly swaps both the reference-image row and the price/promo/copy fields in both directions (extracted vs. not-extracted), and the underlying image resource itself loads correctly (confirmed via a direct `new Image()` load bypassing React) — the `naturalWidth: 0` readings on the live `<img loading="lazy">` elements turned out to be a pre-existing, app-wide automated-browser-tool artifact (every Drive-hosted image in the whole app shows the same reading, including long-established ad thumbnails), not a real defect.
- **Committed and pushed**: Part B as `20cbf8d`, Part C as `0b300a1`.

## 2026-07-26 — Real ad-generation pipeline (Part D) — last major "still a mock" area (Claude Code)
- **Replaces Studio Step 4 + Gallery's fake `addResult()` mock loop with a real, end-to-end generation pipeline**, merging two n8n workflows that were never combined in n8n itself: `3. 최종 이미지.json` (text/copy replacement) and `1. 제품 이미지 교체 (완료).json` (product-photo replacement). Every system prompt / instruction string carried over from those workflows is verbatim (or, where explicitly generalized — see below — clearly marked as such in code comments), read directly from the two `.json` workflow files.
- **Model correction, confirmed by a real paid call**: both source n8n workflows and the original task spec called for `gpt-5.5` for text/vision/copy stages and for the top-level Responses API `model` on the render call — but the render call also needs to invoke the `image_generation` tool, and `gpt-4o-mini`/`gpt-4.1` both hit an identical 403 ("organization must be verified") there, exactly like Part B. `gpt-5.5` (not `gpt-image-2` — that's the image model the tool renders through, not something called directly) resolved it, matching Part B's finding.
- **New backend services**:
  - `visionAnalysis.service.js` — `analyzeReferenceAd(imageBase64)`, gpt-5.5 vision via the Responses API (`text:{format:{type:'json_object'}}`, `response.output_text`). Vision Analyst system prompt carried verbatim from `3. 최종 이미지.json`'s text-extraction node, **extended with a 4th task** (`product_instances`, plural, with location/description/perspective/lighting/grip notes) — that workflow never needed to locate the product itself since it only ever did text replacement; this is the `1. 제품 이미지 교체` concept folded in since this pipeline does both swaps from one combined analysis pass.
  - `counterFacts.service.js` — `findCounterFacts(brandKey, extractedTexts)`. Research Specialist system prompt carried verbatim from `3. 최종 이미지.json`'s "DB 검색" node, but de-agentified the same way `analyzeProduct.service.js` already simplified few-shot lookup: one `text-embedding-3-small` embedding + one Pinecone top-5 query (reusing `pinecone.service.js`'s existing `queryFewShot`, which turned out to be exactly the right shape despite its "few-shot" name) instead of a live LangChain tool-calling loop. The system prompt's own "use the Vector Database tool" sentence is technically vestigial under this mechanics but is kept verbatim per instruction.
  - `copywriting.service.js` — `writeReplacementCopy(extractedTexts, counterFacts)`. Lead Copywriter system prompt carried verbatim from `3. 최종 이미지.json`.
  - `renderImage.service.js` — `renderFinalImage(...)`, the actual `responses.create({model:'gpt-5.5', tools:[{type:'image_generation', action:'edit', size}], ...})` call with two `input_image` items (reference ad, then the product's **extracted** reference image from Part B — never the raw marketing photo) and merged `input_text`: the exact text-replacement preservation language carried verbatim from `3. 최종 이미지.json`'s renderer node; the product-swap instruction carried over **in spirit** from `1. 제품 이미지 교체` (that workflow hardcoded one competitor product name and two fixed hand descriptions for one specific ad — this generalizes it using whatever `product_instances` visionAnalysis actually found for the ad in hand); a new (no n8n precedent) 3-tier qualitative mapping of the 0-100 `styleIntensity` slider; and the user's free-text `instructions` appended as-is. `size` mapped from the app's 3 format options to exact gpt-image-2-valid pixel dimensions (1:1→1024x1024, 4:5→1024x1280, 9:16→864x1536 — gpt-image-2 accepts any size meeting its own constraints rather than a fixed enum, confirmed against current OpenAI docs). Reuses `gptImage.js`'s `extractGeneratedImageBase64` (Part B) rather than re-deriving that parsing.
  - `imageIO.service.js` reused as-is from Part B (no changes needed) for both the reference-ad/product downloads and the generated-image upload (`uploadGeneratedImage`, Drive root "AdGen Generated Ads").
- **New "생성결과" tab**: `generatedAd.mapper.js` (9 columns: Generation ID/Brand/Reference Ad ID/Format/Style Intensity/Instructions/Image URL/Status/Created At) + `generatedSheets.service.js`, same lazy-create/header-migration-safe pattern as `productSheets.service.js`. Live-verified: tab auto-created with the exact expected header on first use.
- **`generation.service.js`** — job via the shared `createJobStore()` (same pattern as collect/product-sync jobs, no new job mechanism). Validates the selected product has an `Extracted Image URL` *before* starting (throws with `.badRequest=true` → controller returns 400 with a clear Korean message pointing at 상품관리) rather than failing deep inside an already-running job. Runs the expensive per-reference-ad stages (vision/research/copywriting) exactly once per selected ad and caches the result in memory for that job's lifetime, then loops the render call once per format × quantity combination — per-image failures are collected without aborting the rest of the batch. Progress phases: analyzing → researching → writing → rendering (N/M) → saving → done.
- **`generation.controller.js` + routes**: `POST /api/generate`, `GET /api/generate/:jobId`, `GET /api/generate/results`, `PATCH /api/generate/results/:id`. Dry-run verified (free, before any real call): unknown brand → 400, product with no extraction → 400 with the correct Korean message, missing `refAdIds` → 400.
- **Frontend**: `backendClient.js` gained `startGeneration`/`getGenerationStatus`/`getGeneratedResults`/`updateGeneratedStatus`; new `adaptGeneratedResult.js` (mirrors `adaptAd.js`/`adaptProduct.js` — every row is an already-finished render, so there's no in-progress/failed status to adapt from the sheet itself, only from the active job).
  - `GalleryContext.jsx` rewritten: loads real results on mount, `startGeneration` via the same shared `useJobPolling` hook Products/Ads already use, real `approveResult` (optimistic, rolls back on failure) and `retryResult`.
  - **Retry decision: per-image, not whole-batch** — re-running the whole original request would re-render (and re-pay for) every image that already succeeded, the single most expensive action in the app. What it can't fully avoid: the per-reference-ad analysis cache only lives in memory for one job's lifetime, so a retry after that job finished can't skip straight to rendering — it re-runs the three comparatively cheap (no image-generation-tool involved) analysis calls for just the one failed ad, then renders exactly one image. It never touches any other ad/format/quantity slot from the original batch.
  - `StudioContext.jsx`'s `goNext()`: real `startGeneration()` call, with an explicit pre-flight check (`product.extractedImage`) that surfaces "상품관리에서 먼저 추출해주세요" instead of letting the request fail opaquely server-side. **New judgment call**: the backend's job contract is single-brand-per-request (like every other job in this app — sync/collect/extract are all one-at-a-time too), and the shared `useJobPolling` slot can only track one active job — so with multiple active brands selected in Step 3, generation now runs for the first active brand only, with a toast explaining that. Navigates to the gallery immediately (no artificial delay), same convention as the rest of the app.
  - `ResultCard.jsx`/`GalleryScreen.jsx` rewritten for real data: real generated image, 비교 reuses `ImageLightbox` as-is (comparing the reference ad's original image against the generated one, not rebuilt), 다운로드 is a plain new-tab anchor (same honesty as `ImageLightbox`'s own "원본 이미지 열기" — Drive thumbnail URLs are cross-origin, so a `download` attribute can't force-save from a different origin, only same-origin links can). New `GenerationProgress.jsx` mirrors `SyncProgress.jsx`'s exact shape (phase label + bar + recent-items list) via `studio.css`'s already-global `.sync-*` classes, including inline retry buttons for any failed slots — both while a job is actively running and for a short while after it finishes (a new `lastSummary` state in `GalleryContext` keeps failures + retry buttons visible past the moment `activeJob` clears to `null`).
  - `StepGenerationOptions.jsx` gained a one-line hint clarifying what 스타일 반영 강도 now actually controls.
- **Verified with exactly one real end-to-end run**, per the cost-conscious testing constraint (dry runs and free validation-path checks first; real 안티칼 reference ad AD 1892190571754571, healthykiki product 11 from Part B, format 1:1, quantity 1):
  - First attempt failed at the Pinecone-query stage with a transient network error ("Request failed to reach Pinecone") — confirmed via a direct `getNamespaceStats` call moments later that Pinecone itself was healthy, so this was reasoned through as a cold-start/transient blip (not a code bug) before spending a second real call. No image-generation cost was incurred on this attempt — it failed before the render step.
  - **Second attempt succeeded completely**: job went analyzing → researching → writing → rendering (1/1) → saving → done in ~2 minutes. Downloaded and visually confirmed the actual result: **both swaps worked correctly in one call** — all 4 competitor bottles replaced with the healthykiki Contoul-it product (labels legible, consistent lighting/positioning across all 4 repeated instances), and the "UP TO 81%" badge correctly became "최대 51%" (matching the product's real 51%-discount promo data pulled from the sheet via Pinecone), while every other hook/headline text ("품절주의", "특가는 타이밍", "지금이 찐 마지막 기회!") was correctly left untouched since none of it was a hard fact needing replacement. No prompt tuning was needed beyond what's described above — the merged single-call approach worked on the first fully-completed attempt.
  - Confirmed the Drive upload (public "AdGen Generated Ads/healthykiki/<id>.png" link), the 생성결과 sheet row (all 9 columns correct, Status defaulted to 미승인), and the Gallery UI (via direct DOM inspection, since the browser screenshot tool remains unreliable in this environment) all show the same result: correct thumbnail, correct brand/format/ref-ad metadata, 비교 correctly opens a 2-image lightbox (original ad vs. generated result), and 승인 correctly persists `Status: 승인` back to the sheet.
- Also fixed in passing: `node_modules` had been partially evicted (likely OneDrive Desktop sync/Files-On-Demand behavior on this machine, unrelated to any code change) mid-session, breaking `vite build` with "vite is not recognized" — resolved with a plain `npm install`, unrelated to any app code.
- **CLAUDE.md and this file updated** — per CLAUDE.md's own prior note, this was the last major "still a mock" area in the app. Every pipeline (ad collection, product sync, product-image extraction, ad generation) is now real and live-verified end-to-end.

## 2026-07-27 — Step 3 UX: inline extraction button + editable product overrides (Claude Code)
- **Inline extraction button in `StepMyBrand.jsx`**: the "우리 제품 참조 이미지" card's placeholder note ("상품관리에서 추출하세요") is now a real 추출/다시 추출 button, reusing `ProductsContext`'s existing `extractImage`/`extractingIds` — no new extraction logic, same code path 상품관리's `ProductDetailModal.jsx` already uses. 상품관리 keeps its own button too; this just avoids tabbing away mid-Studio-setup. Not re-tested with a real paid call here — it's the identical, already-verified (Part B) function.
- **Editable Price/Promotion Info/Ad Hook Copy**, solved with the same column-ownership split `EXTRACTION_COLUMNS` used for the extracted image: these three fields are all in `product.mapper.js`'s `SYNC_COLUMNS`, rewritten by every Cafe24 resync, so a manual correction written into those cells directly would get silently wiped on the next sync.
  - `product.mapper.js`: new `OVERRIDE_COLUMNS = ['Price Override', 'Promotion Info Override', 'Ad Hook Copy Override']`, appended after `EXTRACTION_COLUMNS` — `PRODUCT_COLUMNS` is now 17 columns (A..Q). `mapProduct()` never produces these keys. Test suite updated (17-column row-length + position assertions, plus an assertion that `mapProduct`'s output never contains any override key).
  - `productSheets.service.js`: `LAST_COLUMN` bumped to `'Q'`. **Verified rather than assumed** that the existing header-migration logic and `SYNC_LAST_COLUMN`-restricted resync range both still work unchanged — both are already derived generically from `SYNC_COLUMNS.length`/`PRODUCT_COLUMNS.length`, not hardcoded to the old column count. Confirmed live against the real sheet: triggered the migration, read the raw header back directly (`'제품'!A1:Q1`) — exactly the expected 17 columns, existing 14 columns' positions and product 11's existing extraction data both untouched. New `updateProductFields(productId, fieldsObj)` batches multiple cell writes into one `values.batchUpdate` call (not one round-trip per field), mirroring the same row-lookup `updateProductField` already uses.
  - New `PATCH /api/products/:brand/:productId/fields` — validates the brand exists, the product exists under that brand, and every field key is one of the three editable `*_Override` columns (400 otherwise). Dry-run verified (free): unknown brand → 404, empty body → 400, non-override field name (`"Price"`) → 400 naming the allowed set, unknown product → 404. Then verified the real write path: a real two-field batch update round-tripped correctly through the sheet.
  - `adaptProduct.js`: now exposes *effective* values under the same field names as before (`priceFormatted`/`promotionInfo`/`adHookCopy` — override if non-empty, else the synced value) so nothing downstream needed to change, plus new raw `*OverrideRaw` fields an edit UI can check to tell "currently overridden" apart from "just showing the synced value by coincidence."
  - `ProductsContext.jsx`: `groupByBrand` extended to carry the raw synced price and all three `*OverrideRaw` fields through to Step 3's per-product dict. New `updateProductFields(brandKey, productId, fields)` action — same non-optimistic, update-after-success pattern `extractImage` already uses (re-runs `adaptProduct` on the merged raw row rather than re-deriving the override-precedence logic a second time).
  - New `StepProductFields.jsx`: Price/Promotion Info/Ad Hook Copy are now inputs with a single "저장" button that only sends whichever fields actually changed in one batched PATCH, plus a "수정됨" badge (new `.badge.edited` variant) next to any field currently overridden. Pulled into its own component (not inlined in `StepMyBrand`'s `activeBrands.map()`) so its local edit-buffer state follows the Rules of Hooks correctly. Removed the now-dead `.readonly-value` CSS rule.
  - **Live-verified end-to-end through the actual UI**, not just the API: set a price/copy override via direct API calls first, confirmed the product card and the info-grid inputs both showed the effective override values with the correct "수정됨" badges (2 of 3 fields, matching what was actually overridden); then edited the Promotion Info input through the real DOM, clicked 저장, and confirmed both the UI (badge count went 2→3, input reflects the new value) and the underlying sheet updated correctly. Test overrides cleared back to empty afterward so the real sheet doesn't carry placeholder test values.
- **Known, explicitly-flagged limitation, not solved in this pass**: overrides only affect what's *displayed* (and what `generation.service.js` reads from the sheet for the render pipeline) — they do **not** re-embed into Pinecone. `counterFacts.service.js`'s retrieval during generation will keep surfacing the old synced Promotion Info until the next full resync re-embeds with whatever's in the sheet at that time, which itself would need `mapProduct()` to read overrides too (a bigger change, deliberately out of scope here).
- Backend tests (11/11) and frontend build both pass throughout; no new gpt-5.5 calls needed for either feature (pure CRUD + UI, reusing Part B's extraction call as-is).

## 2026-07-27 — Cafe24 OAuth moved off local disk, onto the deployed backend (Claude Code)
- **Problem**: the backend is now deployed on Railway, whose container filesystem is ephemeral (wiped every redeploy) — the old flow (`cafe24.client.js` reading/writing a local `backend/data/cafe24-tokens.json`, plus a local `cafe24-auth-exchange.js` script meant to run on the same machine as the backend) would force re-authorization after every single deploy, and can't work at all when whoever completes the browser OAuth consent is a different machine than the one running the backend.
- **New `cafe24TokenStore.service.js`**: same lazy-create/header-migration-safe tab pattern as `productSheets.service.js`/`generatedSheets.service.js`. New "cafe24_tokens" tab in the same spreadsheet (Brand Key/Access Token/Refresh Token/Expires At/Refresh Expires At/Updated At), one row per brand, upserted by Brand Key. **Dry-run verified for free**: hit `/api/products/status` locally, confirmed the tab was created with exactly the expected 6-column header via a direct Sheets read.
- **`cafe24.client.js`** rewired to call `getTokenEntry`/`saveTokenEntry` instead of `fs.readFile`/`fs.writeFile` against the local file — `getAccessToken`/`isAuthorized`/`getAuthorizeUrl`/`exchangeCodeForTokens`'s signatures are all unchanged, so nothing calling into this module needed to change.
- **New `POST /api/products/:brand/cafe24/exchange`** (body `{code}`) — this is what actually makes authorization reach the deployed backend: it calls the existing `exchangeCodeForTokens` directly on whichever machine is running this Express process, rather than a local script writing to a machine the backend never reads from. Failures (expired/reused code, wrong brand) return 400 with Cafe24's own error text.
- **`Cafe24CallbackPage.jsx`** no longer displays a copyable `node scripts/cafe24-auth-exchange.js <brand> <code>` command — it now POSTs the code straight to the new endpoint via `fetch`, against the same `VITE_API_BASE_URL` `backendClient.js` already uses (newly exported from there so this deliberately-dependency-light page doesn't have to duplicate the env-var-with-fallback logic). Shows a real loading → success/failure state on the page itself.
- **`scripts/cafe24-auth-exchange.js` deleted** — fully replaced by the callback page's fetch call. `scripts/cafe24-auth.js` still works unchanged as the URL-printing step; its usage comments updated to describe the new one-step flow (approve in the browser, the callback page finishes it — nothing to copy or run locally anymore).
- **`.env.example` updated** (no project-level README exists — this is where the Cafe24 setup flow was already documented) to describe the new flow and the token store's new location.
- **Real deployment-configuration gap found and flagged, not something my code could fix**: verifying this end-to-end against the *live* Vercel-deployed frontend surfaced that `adgen-studio-red.vercel.app` was still calling `http://localhost:4000` for every API request (confirmed via actual network requests in a real browser session) — a mixed-content-blocked, always-broken call for any real visitor. The repo's root `.env.production` already correctly sets `VITE_API_BASE_URL` to the Railway URL (committed earlier today in `b0e113a`, "Point production frontend at the deployed Railway backend"), so the fix isn't a missing file — Vite bakes `VITE_*` vars in at *build* time, and the Vercel deployment simply hadn't rebuilt since that commit landed. This session's own push (below) triggers a fresh build that should pick it up; see the live-verification note for whether that held.

## 2026-07-27 — New reference-images screen + multi-product Step 3 selection (Claude Code)
- **New `추출 참조 이미지` sidebar screen** (`ReferenceImagesScreen.jsx`, wired into `Sidebar.jsx`/`App.jsx` right after 상품관리): one card-grid section per brand showing every product's original photo, extracted image (or an empty state), extraction timestamp, and an extract/재추출 button — this is the exact `.prod-refs` markup `StepMyBrand.jsx` already renders for one product, reused unmodified via `useProducts()`'s existing `extractImage`/`extractingIds` rather than inventing parallel markup for the same "원본 vs 추출" pair. No new backend endpoint needed. `StepMyBrand.jsx`'s own extraction button/badge re-checked after this and confirmed unaffected.
- **Step 3 product selection changed from single-select to multi-select**, since a brand often has more than one product worth generating for in the same batch:
  - `StudioContext.jsx`: `productOverrides[brand]` changed shape from `{ product }` to `{ products: [...] }`. `setProductName` replaced with `toggleProductSelection(brandName, productName)`, which materializes the implicit "first product" default into an explicit array before toggling (needed so clicking a second product while still on the un-touched default *adds* to the selection instead of silently replacing it) and refuses to let the array go empty — a brand must always have at least one product selected once active. `goNext()` now resolves the full selected-products array, blocks with a toast **naming every product** missing an extraction (not just the first), and sends `brand: { key, productIds: [...] }` (plural) instead of a single `productId`.
  - `StepMyBrand.jsx`: product cards are now a real multi-select (all selected cards stay highlighted, native `<select multiple>` fallback at 8+ products), hint text updated to "여러 개 선택 가능", and a `>1 개 선택 시` summary line warns that render count scales with product count. Everything below the picker — the reference-image pair and `StepProductFields` — is now rendered once per selected product via `.map()`, reusing the identical single-product markup unmodified; confirmed `StepProductFields` (already keyed on `product.productId` for its local edit-buffer state) needed zero changes to work correctly inside the loop, since each invocation is a genuinely separate component instance.
- **Backend fan-out for multi-product generation, deliberately multiplying real render cost (an accepted tradeoff, not a bug)** — `generation.service.js`:
  - New pure `computeTotalRenders(products, refAds, formats, quantity)` helper (unit-tested without touching any real API).
  - `prepareInputs` (now exported, dependency-injectable the same way `analyzeProduct.service.js` established) resolves every `brand.productIds` entry against that brand's product list — unresolvable IDs, or IDs belonging to a different brand, throw a `badRequest` naming exactly which ones; any resolved product missing `Extracted Image URL` throws naming which product(s) by name. This mirrors `StudioContext.jsx`'s own pre-flight check but enforces it independently server-side, since the frontend can't be trusted as the only gate on a real-money endpoint.
  - `runJob` restructured so the expensive per-reference-ad stages (vision/research/copywriting) still build exactly once per ad regardless of product count (that cache is ad-keyed, not product-keyed), then loops products on the *outside* — downloading each product's reference image once per product, not once per render — with the existing ad × format × quantity loop nested inside. `progress.recentItems` and `summary.failures` entries now carry `productId` so multi-product batches stay attributable in the UI.
  - `generatedAd.mapper.js` gained a `Product ID` column (appended at the end, 10 columns total, same append-only convention as `OVERRIDE_COLUMNS`); `generatedSheets.service.js`'s `LAST_COLUMN` bumped to `'J'` — the existing header-migration logic needed no changes, being already generic over column count.
- **Consequential fixes required by the `productId` → `productIds` payload shape change**, found by re-reading (not just editing) every caller: `generation.controller.js`'s request validation still checked the old singular `brand.productId` (now validates `brand.productIds` as a non-empty array); `GalleryContext.jsx`'s `retryResult` was still spreading `lastParams.brand` wholesale, which would have silently re-fanned-out a single-image retry across every product in the original batch instead of just the one that failed (now scopes `brand.productIds` to `[failedItem.productId]`); `GenerationProgress.jsx`/`GalleryScreen.jsx`'s progress/failure rows now display `productId` alongside ad/format, since with multiple products in flight the old shape was no longer enough to tell results apart in the live feed.
- **Tests added** (`backend/test/generatedAd.mapper.test.js`, `backend/test/generation.service.test.js`; backend suite now 22/22 passing): new-column position/mapping/`toRow` assertions; `prepareInputs` resolving multiple real productIds, throwing (naming the ID) on an unknown or wrong-brand productId, throwing (naming the product) on an unextracted one, and rejecting an empty `productIds`/unknown brand; `computeTotalRenders`'s multiplication. All against injected fake product/ad lists — no real API calls in the test suite itself.
- **Verified with one real, bounded end-to-end run** against the local backend (2 products × 1 reference ad × 1 format × quantity 1, the agreed cap): healthykiki products 18 ("컨투올잇" discount-link listing) and 11 ("컨투올잇 Contoul-it"), reference ad AD 1892190571754571. First two attempts failed at the Pinecone-query stage with `ConnectTimeoutError` from Node's `fetch`/undici specifically (confirmed via direct probing: `curl` and Node's plain `https` module both reached the same Pinecone data-plane host fine, only `fetch` timed out) — a transient, environment-local networking quirk unrelated to this feature's code, not retried blindly; a follow-up connectivity probe minutes later succeeded consistently, and the third attempt completed cleanly. Result: both renders succeeded and landed as two distinct 생성결과 rows with correct, distinct `Product ID`s (`18` and `11`) and distinct Image URLs — confirmed by reading the rows back from the sheet directly, not just trusting the job's own summary. Real API calls spent: 3 vision-analysis + embedding attempts (2 aborted pre-render by the Pinecone issue above, cost-wise minor since neither reached the image-generation step) plus 2 completed full renders (the intended, bounded cost).
- Frontend build and backend test suite (22/22) both pass.
