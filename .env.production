# Production backend URL (Railway) — baked into the Vite build at build time.
# Not a secret (just a public API base URL), so this is safe to commit, unlike
# backend/.env's actual credentials.
VITE_API_BASE_URL=https://backend-production-5a23.up.railway.app
